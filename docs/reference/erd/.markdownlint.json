{
  "//": "Scoped to this directory so the rule keeps its teeth everywhere else. These four files are GENERATED by `prisma-erd-generator` from each service's schema.prisma — docs/README.md says 'Never hand-edit' — and the generator opens them with a bare ```mermaid fence. Adding an H1 by hand would be correct markdown and would be overwritten by the next `prisma generate`, so the exemption belongs here rather than in the files.",
  "MD041": false
}
