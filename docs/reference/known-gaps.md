# Known Gaps — Read Before You Trip Over Them

**Current state.** Real, current, and they bite silently. Each row names the file that proves it. And when a gap is closed, just add ***Closed.*** to indicate it's closed.

| # | Gap | Where | Consequence |
| :---- | :---- | :---- | :---- |
| 1 | **Tenant scoping is applied by hand.** `tenantScope()` is the shared helper and is used across services, but there is **no Prisma middleware or `$extends`** — nothing forces a query to call it. | `libs/common/src/utils/tenant-scope.ts` | A forgotten `...tenantScope(ctx)` is a cross-tenant data leak, not a bug. **`findUnique({ where: { id } })` cannot express the filter at all** — always `findFirst` + scope, and let a miss be `NOT_FOUND`, never `PERMISSION_DENIED`. |
| 2 | ***Closed.*** Both human-typed code columns now use a salted **scrypt** hash — `hashCode()` / `verifyCode()` in `crypto.ts`, `N=16384, r=8, p=1`, measured at ~33 ms per hash — called from `otp.service.ts` and `two-factor-auth.service.ts`. The by-value token columns keep SHA-256 (`hashToken`), which is correct for them: they are found by an indexed equality lookup, which a salted hash cannot support. The number is kept rather than reused so citations of `#3` and above stay valid. | `apps/auth-service/src/common/utils/crypto.ts` (`hashCode`, `CODE_HASH_PARAMS`) | — |
| 3 | ***Closed.*** Deferred ingestion jobs are re-queued by `IngestionReconcileSweep` (`ingestion-reconcile`, every ten minutes), which also recovers the case this row never named: a `document.uploaded` lost to core NATS, where the job row sits `QUEUED` with `bullmqJobId: ''` and nothing was ever enqueued. The number is kept rather than reused so citations of `#4` and above stay valid. | — | — |
| 4 | ***Closed.*** At most one live ingestion job per document is now enforced by `ingestion_jobs_one_live_per_document`. The number is kept rather than reused so citations of `#5` and above stay valid. | — | — |
| 5 | ***Closed.*** `users_super_admin_iff_no_tenant` — `CHECK ((organization_id IS NULL) = is_super_admin)` — is applied by auth-service's seeder DDL block (`applyCheckConstraints`, ADR 0039), the same `DO $$ … IF NOT EXISTS` shape as the lock CHECK, so `db push --force-reset` cannot lose it. Pre-flight was 0 violating rows on every database, soft-deleted included. It is an iff and both directions are tested by raw insert under Prisma's types, plus both legal pairs — a suite that only checked refusals would pass against `CHECK (false)`. `schema.prisma`'s comment now names the constraint and its producer; `AuditPublisher.record`'s `!== undefined` rests on an enforced invariant. The object count in `schema-contract.spec.ts` is 24 and the digit is gone from every editable prose copy — which turned out to be ten places, not the five the spec's own comment listed. Original text follows. **The `users` super-admin CHECK constraint was never applied.** `apps/auth-service/prisma/schema.prisma:151` says `organization_id` is *"kept in lockstep with isSuperAdmin by a DB CHECK constraint added via migration: (organization_id IS NULL) = is_super_admin"*, There are no migrations, and the seeder's DDL block creates exactly one CHECK on `users` — `users_locked_until_requires_lock` — which is a different constraint. `development-conventions.md` §7 used to name this constraint as one of two exceptions, which is how a constraint that does not exist was documented as existing for as long as it was; §7 now points at the seeder blocks instead (ADR 0039), so the block is the only place to read its absence. | `apps/auth-service/prisma/schema.prisma:151`, `apps/auth-service/src/modules/prisma/database.seeder.ts` (the raw-DDL block), `docs/development-conventions.md` §7 | Nothing prevents `organization_id = NULL` with `is_super_admin = false`: a row with no tenant that is not a platform account, which every tenant filter treats as unreachable rather than as an error. Any code reasoning "super admins have no tenant" — e.g. the `!== undefined` guard in `AuditPublisher.record` — is relying on convention, not on the database. |
| 6 | ***Closed.*** **`?searchTerm=` was advertised on five list routes and honoured on none of them** — and on one GraphQL query. Seven `List*QueryDto` classes extended `SearchPaginationDto`; two (`documents`, `knowledge-articles`) were honoured and five — ingestion jobs, document flags, audit logs, ticket messages, feedback — ignored it, while `IngestionJobsArgsGqlDto` inherited `searchTerm` from `PageArgsGqlDto` and `ingestion-jobs.resolver.ts` forwarded it to a service that dropped it. **Closed per route, by whether a column is worth searching:** messages now filter `content` and feedback filters `feedbackText`; audit logs, ingestion jobs and document flags moved to a new `PaginationDto` base and **refuse** `searchTerm` with a 400 naming the property (`forbidNonWhitelisted`). `PageArgsGqlDto` split the same way — `SearchPageArgsGqlDto` for the four honest resolvers, the plain base for ingestion jobs — and the committed SDL diff is one line, `searchTerm: String` leaving `ingestionJobs`. `dto-contract.e2e-spec.ts` now asserts the invariant that let this happen: **GraphQL never advertises a filter REST refuses** — directional, because `tickets` legitimately carries `searchTerm` on REST only, which is the *subset, not a mirror* direction the read-side rule allows. | `apps/api-gateway/src/common/dto/rest/pagination.dto.ts` (also the ADR 0032 clearing record), `search-pagination.dto.ts`, `apps/api-gateway/src/common/dto/graphql/page-args.gql-dto.ts`, `apps/ticket-service/src/modules/messages/messages.service.ts`, `feedback.service.ts` | A documented filter is now either applied or refused, never swallowed. The earlier text of this row said `?search=` (the parameter is `searchTerm`) and "exactly two services" (`toSearchFilter` had eleven call sites across eight) — both corrected here rather than appended to. |
| 7 | **`scheduler-ownership.e2e-spec` fails intermittently on a full auth-service run.** Three sightings across three docs, each on `npm run test:e2e:auth` with every other suite in the run, each passing in isolation on the next attempt, and each with the session's changes nowhere near a scheduler. The suite asserts BullMQ queue isolation against a **shared real Redis**, and every service's e2e suite uses the same instance — `bootstrap.ts` already documents the class of problem for ingestion ("WORK must not escape its test"). | `apps/auth-service/test/modules/scheduler-ownership.e2e-spec.ts`, `apps/auth-service/test/utils/bootstrap.ts` | A red suite that is green on re-run teaches the team to re-run. **FOURTH SIGHTING, CAPTURED.** Failure: the ticket assertion, with `analytics-daily` seen TWICE — one job processed twice, not a missing one. Redis at that moment held live repeat schedulers left by OTHER suites' app bootstraps: `bull:scheduler-ticket:repeat:repeat-analytics-daily` (+1 delayed job) and `bull:scheduler-auth:repeat:repeat-auth-{hourly,daily}` (+2 delayed). Those are written by each service's `SchedulerRegistrar` at bootstrap and **nothing removes them at teardown**, so they outlive the suite that created them in the shared Redis. The duplicate carries the name of a leftover repeat entry, on the queue that leftover sits on. The spec itself is not the leak — run in isolation it passes AND obliterates all three scheduler queues in `afterEach`, which is why the leftovers came from elsewhere. **Not proven end to end:** the delayed jobs' next-fire times were in the future at capture, so promotion into the test window is the step still unshown. **Likely fix:** each service's e2e teardown obliterates its own `SCHEDULER_QUEUE`, so one suite's registrar cannot leave a live schedule for another suite's worker. **SIXTH SIGHTING, CAPTURED, AND THE UNSHOWN STEP IS NOW SHOWN.** Same spec, same test, same signature — `analytics-daily` seen TWICE on `scheduler-ticket` during a full `test:e2e:auth`. Redis was captured before any re-run this time. What it held: `bull:scheduler-auth:repeat:repeat-auth-{hourly,daily}` plus their delayed jobs, with `repeat-auth-hourly` next-firing at `06:00:00.000Z` — and at `06:00:11Z`, eleven seconds PAST that time, the delayed job was still sitting there unprocessed. **Nothing promotes a leftover in the background, because no worker is alive between runs.** Probe: with the stale `…:1787983200000` job present, an unrelated suite (`platform.e2e`) was run; afterwards that job's hash had grown from 7 fields to 12 (`processedOn`, `finishedOn`, a return value) and the schedule had advanced to `07:00` with a fresh delayed job. So promotion is **bootstrap-driven, not time-driven**: any leftover whose fire time has passed is promoted and PROCESSED by the next suite that boots a worker on that queue, whichever suite that happens to be. That is the missing link — it explains why the failure needs a full sequential run (many bootstraps, and long enough to cross a fire boundary) and why an isolated re-run is green (a fresh obliterate and a short window). The fix shape is unchanged and now has a reason rather than a hunch: teardown must obliterate its own `SCHEDULER_QUEUE` because it is leaving behind a schedule that another suite will EXECUTE, not merely a key that another suite will read. **SEVENTH SIGHTING, not re-captured deliberately.** Same spec, same test, on a full `test:e2e:auth` whose changes were confined to notification-service. The mechanism is settled — the sixth sighting demonstrated bootstrap-driven promotion end to end — so a further capture would add a date and nothing else. What this sighting adds is frequency: it now appears on roughly every second full auth run, which is the argument for doing the teardown fix rather than continuing to log it. **FIFTH SIGHTING, and the capture instruction was not followed.** Same spec, same test — *"each service receives its OWN jobs and none of its neighbours'"* — failing on a full `test:e2e:auth` inside a seven-suite sequential sweep, passing on its own immediately after. What was NOT captured is the thing this row asks for: Redis state at the moment of failure. It was re-run before being inspected, which is precisely the reflex the row exists to break, so this sighting adds a date and nothing else. **The instruction needs to be actionable to be followed:** "capture before re-running" competes with a sweep that has already moved on to the next service by the time anyone reads the output. A `--runInBand` sweep that dumps `KEYS bull:scheduler-*` into the log on failure would collect it without anyone remembering to. ***CLOSED — the teardown fix landed, and it was TWO files rather than four.*** A census of the four scheduler-owning bootstraps found ingestion and notification already obliterating their own queues at reset; auth's only scheduler reference was an `import` of `SchedulerProcessor` and ticket had none — **the two without it are exactly the two whose leftovers the sixth sighting captured**. Both now obliterate in `close()`, before the module tears down. Measured with the fix reverted as a control: a single `test:e2e:ticket` left **46** bull keys including `bull:scheduler-ticket:repeat:repeat-analytics-daily` — the key named above — and **0** with it restored. Acceptance was a rate, not a pass: one ticket run followed by **six consecutive `test:e2e:auth` runs, six greens, 471/471 each**. The capture instruction is answered too, run-scoped rather than spec-scoped per #13's correction: `scripts/capture-e2e-state.mjs`, called by both a developer and the CI job's `if: failure()` step. **A caution the first attempt earned:** the probe was initially `redis-cli` piped into `wc -l`, and `redis-cli` is not installed on the machine that runs these suites — so an error message counted as zero lines and "no leftovers" was indistinguishable from "no tool". The capture script now falls back to `docker exec` and reports absence as absence. |
| 8 | ***Accepted, not planned.*** The staleness is bounded by `ENTITY_TTL_SECONDS` and is the same trade `CacheInvalidationInterceptor` accepts one line away for a platform Super Admin writing into a tenant. Closing it needs a new NATS subject end to end — a contract (no `INVITATION_PATTERNS` exists), a publisher in auth-service (`invitations.service.ts` injects `NotificationPublisher`, a different thing), a consumer branch in the gateway — to remove five minutes of staleness on one screen. **The condition under which this row reopens** is an invitation-accepted event being wanted for its own sake by something else; then this eviction is its first consumer, not its justification. Original text follows. **Three writes move `roles.user_assigned` without evicting the cache that shows it.** `GET /roles` caches `userAssigned` for five minutes per tenant. `PUT /users/:id/roles` and `DELETE /users/:id` now evict it; **`POST /invitations/accept` cannot** — `CacheInvalidationInterceptor` derives the tenant from the request context and returns early without one, and that route is unauthenticated by design. | `apps/api-gateway/src/modules/invitations/invitations.controller.ts` (`accept`), `apps/api-gateway/src/common/interceptors/cache-invalidation.interceptor.ts` | After an invitation is accepted, the roles screen shows a stale `userAssigned` for up to five minutes, and a cached hit is indistinguishable from a fresh read. Covered by TTL, which is the same trade that interceptor already accepts for a platform Super Admin writing into a tenant. Closing it needs an eviction path that does not depend on a caller — the NATS consumer that class mentions. |
| 9 | ***Closed.*** The family is `Export`: model, `ExportKind`/`ExportStatus` (common and proto — `buf lint`'s `ENUM_VALUE_PREFIX` moved the value prefixes too, so `proto:breaking` reports six wire-compatible flags rather than two), `EXPORT_QUEUE`, `export.{facade,processor,service}.ts`, `ExportResponseDto`, and all seven config exports under `export.config.ts`. **The table kept its name** — `@@map("analytics_exports")` with the reason beside it: a `RENAME TABLE` is an ADR 0044 migration that breaks the previous image mid-rollout and buys a client nothing. Do not re-file that as a gap. Original text follows. **The export family is named for one of its four kinds.** `AnalyticsExport`, `analytics_exports`, `AnalyticsExportKind`, `ANALYTICS_EXPORT_QUEUE` and `apps/ticket-service/src/modules/analytics/analytics-export.*` now carry `TICKET` and `AUDIT_LOG` exports, which read live tables and are not analytics. The shared thing is the LIFECYCLE — one status machine, one queue, one processor, one poll route — which is why they share a table rather than getting two more. | `libs/common/src/configs/analytics.config.ts` (`AnalyticsExportKind`), `apps/ticket-service/prisma/schema.prisma` (`AnalyticsExport`) | A reader looking for the ticket export finds it under analytics, and a table called `analytics_exports` holds audit-log rows. Renaming to `Export` is ~18 files outside generated code plus a proto regeneration and the enum-bridge registry — deferred deliberately, not overlooked. Do it as its own change, or the half-done state sits under three features. |
| 10 | ***Closed.*** The boot report names the holders: `seedPermissions` runs one query over the implicit `_role_permissions` junction, gated on there being any retired code, and `report()` warns one line per role — name, id, tenant (or `platform`), and the dead codes it still grants. **Nothing on the tenant side changed, deliberately:** `PermissionResponseDto.isRetired` and the GraphQL `Role.permissions` edge already carry the marker, and `role.proto` refuses a second derivation that could disagree with the first. The report is empty while `PERMISSION_CODES` and the table match, which the e2e pins; the retired row has to be manufactured to see it fire. Original text follows. **Nothing reports which roles still hold a RETIRED permission code.** A code removed from `PERMISSION_CODES` is deliberately left in the `permissions` table, because tenant custom roles may still reference it and dropping the row would cascade that grant away silently (ADR 0038). `SeedSummary.retiredPermissionCodes` names the CODES at boot; nothing names the roles. | `apps/auth-service/src/modules/prisma/database.seeder.ts` (`seedPermissions`), `apps/auth-service/src/modules/roles/roles.service.ts` (`listPermissions`) | After a retirement an operator can see that a code is dead and not who is still carrying it — so a role keeps advertising a capability no guard will ever satisfy, and nobody is told which roles to edit. The query is one line: `role.findMany({ where: { permissions: { some: { code: { in: retired } } } } })`. Latent until the first removal — the table and the array match exactly today. |
| 11 | **A re-run re-embeds the whole document and is billed for it.** Every entry into `process()` runs `writeChunkRows`, which deletes and recreates every chunk row, so the `vector_point_id IS NULL` filter in `embedAndUpsert` matches the entire document and no embedding work carries across runs. A retry after a failure in the last batch of a 400-page handbook re-embeds all 400 pages, metered as `EMBEDDING` against the tenant's budget. | `apps/ingestion-service/src/modules/ingestion/ingestion.processor.ts` (`writeChunkRows`, `embedAndUpsert`) | The tenant pays twice for one document, and the charge is invisible — it looks like ordinary ingestion spend. Distinct from #3, which is about a deferred job never being re-enqueued at all; this is about what a run that DOES happen redoes. Resuming across runs needs the point ids to survive `writeChunkRows`, which is a different shape of pipeline, so `ingestion-pipeline.e2e-spec` test 19 pins the current cost rather than asserting it away. |
| 12 | ***Closed.*** `assigned_by_id` is `NOT NULL` — schema and ticket-service's regenerated `0_init` (the constraint was first exercised as a standalone `ALTER` against a scratch schema: clean on an empty table, refused with one null row present, so it enforces rather than no-ops), proto `optional` dropped, both NATS events narrowed to `string`, both mappers, the gateway DTO, and **both `ticket-notification.consumer.ts` sites** — the branch this row predicted but did not name, in a third service. The unused `assignment.factory.ts` was deleted rather than given a default. `nats-contract`'s null round-trip test kept its purpose and moved to `fromAssigneeId`. Original text follows. **`ticket_assignments.assigned_by_id` is nullable for a case that cannot occur.** The column says *"null when the system assigned it"*, but `writeAssignment` is its only writer and takes `requireActor(context)` — and `load` refuses a caller with no identity, so no assignment can happen without an actor. Same shape as `ticket_status_changes.changed_by_id`, which was made `NOT NULL` for exactly this reason when it was added. | `apps/ticket-service/prisma/schema.prisma` (`TicketAssignment`), `assignments.service.ts` (`writeAssignment`), `assignment.proto:16` (`optional string assigned_by_id`) | A reader takes the null as a supported case and writes a branch for it — `assignment.mapper.ts` already has one (`?? undefined`). Closing it is a four-file chain: schema, proto, mapper, gateway DTO. Deliberately deferred by the change that added the status history, whose scope was ticket reads; the comment was corrected in place so it no longer misleads. |
| 13 | **Two specs fail intermittently on full runs because their ARRANGEMENT assumes an idle machine — and neither is gap #7.** `ingestion-pipeline` 6c and `realtime` Presence 5. Both fail only on a full run and that is where the resemblance stops. #7 asserts BullMQ queue isolation against a **shared real Redis** — genuine cross-suite interference. 6c hangs a `downloadObject` promise it owns and then `waitFor`-polls **its own job row in Postgres** until the status reads `PARSING`; it touches no queue and opens no Redis connection. Its failure is a **poll budget sized for an idle process**, expiring when `--runInBand` gives the box more to do. One sighting each, in two services. | `apps/ingestion-service/test/modules/ingestion-pipeline.e2e-spec.ts` (6c), `apps/api-gateway/test/modules/realtime.e2e-spec.ts` (Presence 5), `realtime.gateway.ts` (`announceConnected`), `@synapsedesk/common/testing/wait` | Filed separately on purpose: merging it into #7 would point the next person at Redis for a test that never opens a connection to it, and merging two rows later is cheap while un-merging a generalization that has already sent somebody to the wrong system is not. **SECOND SIGHTING, CAPTURED, different spec, mechanism identified.** `realtime.e2e-spec` Presence 5 expected `away` and got `online`. `announceConnected` broadcasts `online` to `org:{id}` whenever a connecting client's state CHANGES, and the test connects three clients before registering its listener — so `colleague` receives its own connect-time `online`. On an idle machine those broadcasts land before `waitForEvent` registers; under a full run they can land after, and the listener resolves with the wrong event. Same class as 6c: **a test whose arrangement assumes the machine is idle**, not cross-suite interference — no shared state is involved in either. **Fix shape (both):** make the arrangement wait for the thing it is racing rather than assume it has already happened — drain connect-time presence before listening; widen or remove 6c's poll budget. **Next sighting, capture before re-running:** the elapsed time at failure and what else was running. Note #7's capture instruction is scoped to one spec, which is probably the bug — the information it protects (which suites were in the run) is lost either way. ***CLOSED — both arrangements now wait for the thing they were racing.*** Presence 5 took the FIRST `presence` frame, which on a busy box can be `colleague`'s own connect-time `online` from `announceConnected`; it now waits for the frame carrying the user it is about, through a new `waitForMatchingEvent` helper whose docblock records why `waitForEvent`'s first-frame semantics were the enabler. 6c polls its own job row and never touched Redis, so its fix is the budget the row diagnosed: an explicit 20s, named and reasoned against the file's 30s test timeout rather than nudged, since the CI phase measured these suites running ~2.4x slower under CPU contention — which is what put a 5s default inside the failure window. The capture ask is answered by `scripts/capture-e2e-state.mjs`, run-scoped as this row argued: it records the suites in the run, the elapsed time, the load average and the live jest processes alongside #7's Redis keys and #14's streams. |
| 14 | **A suite that tears down shared broker state can fail an unrelated service's suite mid-run — a third distinct class from #7 and #13.** Caught during the JetStream migration, not in the wild: `jetstream-bootstrap.e2e-spec` deleted the real `AUDIT` and `NOTIFICATIONS` streams in `afterAll`, and ticket-service's audit suite failed with `NatsError: stream not found` on every test. Fixed at the source — that spec now deletes nothing and relies on `ensureStream` being idempotent — so **the row is here for the class, not for a live defect**. The same shape is available to any suite that mutates the one broker, the one Redis or the one Qdrant: #7 is *leftover state* another suite reads, #13 is *timing* on a busy machine, and this is *deliberate teardown* of something shared. | `apps/notification-service/test/modules/jetstream-bootstrap.e2e-spec.ts` (`afterAll`), `libs/common/src/utils/jetstream-bootstrap.ts` | The failure names the victim, never the culprit — the error surfaces in a service whose code did not change, in a run that may not even include the suite that deleted the stream. Filed separately for the reason #13 was: pointing the next person at "leftover schedulers in Redis" for a stream somebody dropped costs more than a fourth row. **Rule of thumb for new suites against shared infrastructure: create idempotently, delete only what you named uniquely.** **SECOND SIGHTING, SAME RUN, OPPOSITE DIRECTION.** `audit-consumer.e2e-spec` did not delete enough: a durable consumer's name is stable by design, so the suite inherited seven unacked messages from an earlier run and spent its first tests draining a backlog while asserting `count() === 1`. Two tests failed on the full run and passed in isolation — the signature of this whole family. Both halves of the rule are load-bearing: leaving shared state alone is not the same as leaving YOUR state behind. **THIRD SIGHTING, AND THE ONE THE RULE CANNOT FIX.** A durable left by a real `ticket-service` RUN — not by any suite — held `audit.record`, and a WorkQueue stream allows exactly one consumer per filter subject, so the whole audit suite failed with `filtered consumer not unique on workqueue stream`. "Delete only what you named" is no defence here: the obstacle belongs to nobody's suite, survives every restart because JetStream persists to disk, and the server's message names neither the subject nor the consumer holding it. Resolved in two places rather than by a rule: `PullConsumerRunner` now asks the server WHICH consumer holds the filter and says so, and `npm run nats:reset` drops the durable streams with their consumers and backlogs. Reach for the reset whenever a broker-backed suite fails wholesale rather than in one test. |
| 15 | ***Closed.*** **Nothing bounds the ingestion worker's memory, and there is no artifact in this repo that could.** The parser holds the whole file as a `Buffer` (`document-parser.service.ts`) at `concurrency: 2` (`ingestion.worker.ts:21`), so two documents plus whatever the parse library allocates are resident at once. `MAX_DOCUMENT_BYTES` is **100 MB**. The heap half landed with the image (`runtime-ocr`'s `NODE_OPTIONS=--max-old-space-size=768`, with a comment stating what that flag does NOT bound: the raw buffers live outside the V8 heap). **The container limit is now set** — `k8s/services/ingestion-service.yaml` requests 768Mi and limits 1536Mi, the pair the image's comment named. It is not a number anybody has to remember: `manifest-contract.spec.ts` check 5 DERIVES the floor from `MAX_DOCUMENT_BYTES`, `concurrency` and the heap cap, so raising any of the three fails the manifest instead of the pod. Measured while writing that check: a comparison against the heap cap alone — the assertion that reads right — accepts 800Mi, which OOMKills on the second concurrent parse. | `k8s/services/ingestion-service.yaml`, `libs/common/src/configs/manifest-contract.spec.ts` (check 5), `docker/node-service.Dockerfile` (`runtime-ocr`), `apps/ingestion-service/src/modules/ingestion/ingestion.worker.ts:21`, `libs/common/src/configs/document.config.ts` (`MAX_DOCUMENT_BYTES`) | "Does a 100 MB document OOM the worker" now has an answer that does not begin with "depends which host it lands on". **What the limit does NOT bound is the aggregate**, and that is the half worth carrying forward: replicas multiply concurrency, so two ingestion pods is four concurrent parses and four times the embedding spend per unit time. The Deployment starts at ONE replica for that reason and says so; `MAX_CHUNKS_PER_DOCUMENT` bounds the spend and is per-document, not per-cluster. `docker-compose.yml` still has no `mem_limit` — it holds infrastructure only, and if it ever gains application services the same pair belongs there. |
| 16 | **OCR availability is a tenant-visible failure and an operator-invisible one.** `OcrService` probes `pdftoppm` and `tesseract` once at boot and, when either is absent, warns once and keeps serving — the run-open rule ADR 0016 argues for. The tenant half is closed: a fully scanned document now refuses with `OcrUnavailable` naming the deployment, a partly scanned one flags the pages it lost, and both are logged at `error` per document rather than only once at boot. **The operator half has no surface to close it against.** There is no health module in `apps/ingestion-service` — nothing to add an indicator to, and gating readiness on a minority capability is the boot-closed behaviour the ADR rejects. There is no metrics surface either: `prom-client` lives only in api-gateway, and `job_last_success_timestamp_seconds` reaches it through `job-metrics.collector.ts` reading the `job_runs` heartbeat table other services write. | `apps/ingestion-service/src/modules/ingestion/ocr.service.ts` (`checkAvailability`), `apps/api-gateway/src/modules/metrics/job-metrics.collector.ts`, `docs/decisions/0016-ocr-is-a-per-page-branch.md` | A deployment built without `--target runtime-ocr` serves every text document correctly and fails every scanned one, and nothing an operator watches goes red — the signal is a boot warning that has scrolled away and per-document `error` lines that only appear once tenants are already affected. **The deployment artifact now exists** (`runtime-ocr` in `docker/node-service.Dockerfile`, with a guard stage refusing any `SERVICE` but ingestion); what is still missing is the reporting channel. An `ocr_available` gauge would follow the `job_runs` pattern — ingestion writes capability state to a table, the gateway's collector exports it — which is a capability-reporting channel rather than a metric, and a larger change than the failure it reports. **Narrowed by the manifests, not closed.** The build side is now structural: `runtime-ocr` carries an `ocr-args` gate that fails any build with `SERVICE != ingestion-service`, `check-ocr-image.sh` asserts all eight language packages are present, and `k8s/services/ingestion-service.yaml` names the target in the comment beside the image. What is still operator-invisible is the case those do not reach: a deployment that pulled a plain `runtime` image under the same tag. A startup probe would catch it and is NOT the answer — gating readiness on a minority capability is exactly the boot-closed behaviour ADR 0016 rejects, and a pod that refuses to start serves zero text documents to avoid failing the scanned ones. The shape that fits is a metric or a label asserting which target built the image, and there is still no metrics surface in this service to hang it on. |
| 17 | **Nothing bounds the DECOMPRESSED size of an office attachment, and `MAX_ATTACHMENT_BYTES` cannot.** `.docx` and `.xlsx` are zip containers, so the 10 MB ceiling bounds the COMPRESSED upload while the decompressed object graph is unbounded — and `MAX_EXTRACTED_TEXT_CHARS` caps the RESULT of the parse rather than the parse itself. **This arrived with `.docx`, not with `.xlsx`**, and mammoth is the exposed half: `convertToHtml({ buffer })` reads the whole document into memory before anything is truncated. **`.xlsx` was supposed to be the safer of the two and is not.** exceljs was chosen for a streaming reader whose early exit would have bounded memory by `MAX_SHEET_ROWS` rather than by the file; measured against 4.4.0, `WorkbookReader` crashes on multi-sheet workbooks (103 failures in 200 runs on three sheets, 0 in 60 on one) at `workbook-reader.js:303`, under every option it accepts. `workbook.xlsx.load()` is reliable (200/200) and materialises the whole workbook, so the row cap is a post-hoc slice. Adding the second format did not widen this gap — both formats now load whole — but it did not narrow it either. | `apps/ingestion-service/src/modules/ingestion/document-parser.service.ts` (`parseDocx`, `parseXlsx`), `libs/common/src/configs/ticket.config.ts` (`MAX_ATTACHMENT_BYTES`, `MAX_EXTRACTED_TEXT_CHARS`) | A 10 MB zip of highly compressible XML expands to far more inside a worker with no memory limit, and the failure is not a clean OOM with a requeueable job — it is GC thrash that presents as the pipeline being slow. **The worker's memory bound now has an address**: the `runtime-ocr` stage of `docker/node-service.Dockerfile` carries the heap flag and its comment names the container limit (1.5 GB) that actually bounds decompressed buffers — set where the container is run, which is the half still pending (#15). A decompressed-byte ceiling inside `DocumentParserService` remains future work and now belongs beside that comment rather than with a "missing artifact". Bounded in practice by `MAX_ATTACHMENTS_PER_MESSAGE`, `concurrency: 2`, and the fact that both parsers are only reachable through an authenticated upload. **Bounded rather than unbounded, as of the manifests.** `k8s/services/ingestion-service.yaml` sets `resources.limits.memory: 1536Mi`, so a zip bomb now ends as an OOMKill with a requeueable job instead of GC thrash on a shared host. That converts the failure from unbounded to loud; it does not make the parse safe, and the row stays open because `convertToHtml({ buffer })` and `workbook.xlsx.load()` still materialise the whole document before anything is truncated. |
| 18 | ***Closed.*** The platform usage surface exists: `IngestionPlatformService.GetPlatformUsage` answers bytes AND document count for many tenants in one query, zero-filled per requested id, and the GATEWAY composes it with auth's seat projection — auth could not, because `ingestion-service` dials auth on every presign and the reverse edge would close a cycle on the identity leaf. `evaluated_dimensions` became dynamic in the process: a leg that does not answer drops its dimensions from that run rather than reporting nobody affected. **A GATE now reuses the same composition with the OPPOSITE failure policy.** `POST /billing/plan` folds the same two legs and refuses with `503` when the usage leg does not answer, because a block that fails open is not a block — degradation is the house rule for the REPORT and must not be copied into the gate. Both sides carry a comment saying so, since they read as if they should agree. The number is kept rather than reused so citations of `#19` and above stay valid. Original text follows. **The plan-apply dry run reports SEAT over-limit and nothing else — storage is invisible to it.** The plan-lowering rule's headline example is *"this puts 12 of 47 subscribers over their storage limit"*, and the projection cannot answer it. `PlanAdminService.overLimit` counts active users plus pending invitations, both `auth-service`'s own tables. Storage used is `ingestion-service`'s to count and is exposed only as `GetStorageUsage`, which is **tenant-scoped from the caller context and calls back into auth for the limit** — so answering it for 47 subscribers would be 47 calls each triggering another, from a caller that has no tenant context to offer. | `apps/auth-service/src/modules/billing/plan-admin.service.ts` (`overLimit`), `libs/grpc-proto/src/proto/synapsedesk/ingestion/document.proto` (`GetStorageUsage`) | **The silent-zero half is CLOSED**: `ApplyPlanResponse.evaluated_dimensions` names the limits a run actually checked (`["seats"]`), so a zero `over_limit_count` can no longer be read as "nobody is affected" for a dimension nothing looked at. What remains open is the check itself. **Not closed by widening `GetStorageUsage`**: a cross-tenant read added beside tenant-scoped RPCs is precisely the leak shape `platform.proto`'s own header warns about — *"one forgotten filter away from leaking"* — so it wants a separated platform surface in `ingestion-service` mirroring the one `auth-service` has, plus a gateway that merges the two projections. Nothing is silently wrong meanwhile: seats are reported accurately, the coverage list says storage was not evaluated, and the plan-lowering rule means an over-storage tenant is refused their next upload rather than losing anything. |
| 19 | ***Closed — by decision.*** A domain-matched registration is **never refused for seats**: the row's own blocker — a user told their workspace exists and will not let them in — is a worse outcome for a tenant one seat over than the overrun is. What it no longer does is cross silently. Both write sites (password registration and the Google first-login domain match) return `seatAlarm: existingOrg ? { organizationId, maxAgentSeats } : null` from their transaction, and `fireSeatAlarm` runs **after commit** with the same `void …catch()` idiom `users.service.ts` and `invitations.service.ts` use — the `.catch()` is load-bearing in a test now (a bare `void` fails auth e2e 1e as a rejected registration rather than a process exit). Founding a workspace evaluates nothing: `null` on that branch, pinned by test 1d. **So `maxAgentSeats` is a hard cap on `invitations.create`, `invitations.accept` and `users.create`, and a soft cap on self-registration — the tenant is told, not blocked.** A future decision to make it hard is one `RESOURCE_EXHAUSTED` throw at each of the two sites, and this row is where that decision gets recorded. Note the cap's name: `seatsInUse` counts every active member plus pending invitations, so an End-User self-registration consumes an *agent* seat; the name misleads, the count does not. Original text follows. **Domain-matched registration takes a seat and checks nothing.** The other two paths that consume a seat — `invitations.create` and `users.create` — both compare `seatsInUse` against `maxAgentSeats`, refuse with `RESOURCE_EXHAUSTED`, and fire the seat alarm afterwards. `register` does neither: a tenant that lists its own domain in `allowedEmailDomains` can be taken past `maxAgentSeats` by self-registration alone. | `apps/auth-service/src/modules/auth/auth.service.ts` (`register`), `apps/auth-service/src/modules/organizations/organizations.service.ts` (`seatsInUse`, `alertOnSeats`) | The usage page reports `used > limit` with no event that produced it, and the tenant is never warned on the way there — the alarm's crossing is skipped along with the refusal. **Closing it is a refusal, and a refusal needs a product answer first:** the user is told their workspace exists and will not let them in, which is a worse experience than the overrun for a tenant one seat over. Not a code decision — that is why it is a row rather than a TODO. |
| 20 | ***Closed.*** Held two ways. **By construction:** `createPortalSession` passes `configuration:` explicitly — the configuration `provision-stripe.mjs` creates carrying `metadata.synapsedesk_portal` — so a default configuration created in the Dashboard is never the one a tenant lands in. **By check:** `portalConfiguration()` lists, picks the marked one, and refuses with `FAILED_PRECONDITION` (logged at `error`, thrown before the `try` so `asClientError` cannot flatten it to `UNAVAILABLE`) when nothing is marked or `features.subscription_update.enabled` is `true`; the script refuses in the same two cases on every run. Cached `PORTAL_CONFIGURATION_TTL_MS` (five minutes): a Dashboard flip is enforced within that window, and a Stripe outage inside it keeps the last known-good answer rather than refusing every portal open. **One operator step:** an existing *unmarked* configuration is left alone, so `stripe:provision:apply` must run once to create the marked one before the portal will open — the dry run says so. Original text follows. **The plan-change block's integrity depends on a Stripe Dashboard setting nothing in this repo verifies.** `subscription_update.enabled = false` is written by `scripts/provision-stripe.mjs` at provisioning time and read back by nothing — and the script itself only WARNS when a configuration already exists, leaving it untouched. If the flag is flipped in the Dashboard, or a second portal configuration is created without it, a tenant changes plan inside Stripe's portal, the webhook arrives, `EntitlementWriterService` applies the new grants, and **`POST /billing/plan`'s over-limit block is bypassed with no error anywhere.** | `scripts/provision-stripe.mjs` (the portal configuration), `apps/auth-service/src/modules/billing/billing.service.ts` (`createPortalSession`) | The one enforcement point for "a downgrade is refused when current usage does not fit the target plan" sits outside this repo's control, and its failure is silent and indistinguishable from a plan change the endpoint allowed — the tenant simply lands over a limit. **This is the price of departing from Stripe's own advice**, which is to use the Customer Portal for self-service plan changes; the block cannot run inside Stripe's UI, because a change made there reaches this system only AFTER Stripe has applied it. *Closing it:* read the portal configuration in `createPortalSession` and refuse — or log loudly — when `subscription_update.enabled` is true. |
| 21 | ***Closed.*** `PlanSubscriberProjection.after` (`map<string, int64>`) carries the numeric grants; the composer reads it and the `split('->')` parser is gone. **The fix nearly re-delivered the defect:** four of the seven grants are Prisma `BigInt`, so a `typeof === 'number'` guard would have dropped `maxStorageBytes` and left the storage check silently dead — the guard is `number \|\| bigint` with `Number()`, `plan-usage.composer.spec.ts` test 6 pins that branch verbatim, and `plan-admin.e2e-spec.ts` proves a real `bigint` crossed the wire as a `number` (the gateway suite stubs `applyPlan`, so it cannot). Original text follows. **The plan-apply dry run parses a DISPLAY STRING, and a cosmetic edit silently disarms it.** `enrichWithUsage` reads the `after` half of `"before -> after"` out of the projection DTO and treats a failed parse as "this column is not changing", which means "no check". The separator is a contract between two services — `plan-admin.service.ts` writes it, the gateway's composer reads it — and nothing typed it. Measured: changing `" -> "` to `" → "` in the producer dropped storage and document overruns from every dry run while all eleven `platform-plans` e2e tests stayed green. | `apps/api-gateway/src/modules/platform/plan-usage.composer.ts` (`afterValue`), `apps/auth-service/src/modules/billing/plan-admin.service.ts` (`diff`) | A Super Admin lowering a plan limit is told fewer tenants are affected than actually are, and the under-report is silent. **Half-closed:** `plan-usage.composer.spec.ts` test 2 now asserts the producer still writes the separator the parser expects, so the reformat is red rather than invisible. What remains open is the shape — the projection should carry the target grants as NUMBERS rather than as a rendered string, which is what the plan-change block does and why it is not exposed to this. That is a proto change to `ApplyPlanResponse`, deliberately not folded into the plan-change work. |
| 22 | **`creditIssued`'s premise is unverified against a live Stripe downgrade, and it is a claim about money.** `prorationCredited` reads `latest_invoice.total < 0` off the subscription `subscriptions.update` returns with `expand: ['latest_invoice']`. `proration_behavior: 'always_invoice'` does not guarantee the proration lands there: Stripe may issue a downgrade credit as a customer credit balance transaction, leaving `latest_invoice` pointing at the PREVIOUS invoice — so `false` can be returned for a change that credited, and `true` can be read off an unrelated negative invoice on a straight upgrade. The e2e test asserts the mapping against a MOCKED `update`, so it pins the shape and not the premise. | `apps/auth-service/src/modules/billing/plan-change.service.ts` (`prorationCredited`), `libs/grpc-proto/src/proto/synapsedesk/auth/billing.proto` (`credit_issued`) | A tenant is told they were refunded when they were credited, or the reverse, on the page about their bill. **Bounded rather than open**: the field is three-valued — absent means *not determined* — so a client omits the sentence instead of printing a confident wrong one, and only an expanded invoice produces an answer at all. *Closing it:* one sandbox downgrade with a real card, inspect what `latest_invoice` actually holds, then narrow the field to a plain `bool` as a deliberate edit. Nothing in this repository can settle it. |
| 23 | **The FCM error-code mapping is verified against a fixture, not against Firebase.** `isDeadTokenError` deletes a stored device token for exactly two codes — `messaging/registration-token-not-registered` and `messaging/invalid-registration-token` — and keeps it for everything else. There is no client in this repository (seven services, no mobile app) and no live FCM in any test environment, so the mapping is asserted at a mocked `sendEachForMulticast` boundary: the tests prove the code BRANCHES correctly on those strings, not that those strings are what Firebase actually sends. | `apps/notification-service/src/modules/in-app/in-app-notification.service.ts` (`DEAD_TOKEN_ERRORS`), `apps/notification-service/test/modules/push.e2e-spec.ts` | This is the one path where the system destroys a stored credential because a third party said so. **The two directions are not symmetric**: a code wrongly absent from the set means a dead token lingers and every send to it fails harmlessly, while a code wrongly present means a live device is silently unsubscribed with no error and no way back except reinstalling the app — which is why the set is a literal rather than a prefix match, and why an unknown code KEEPS. Same shape as #22's `creditIssued`: the premise cannot be settled from this repository. *Closing it:* one send against a real FCM project with a deliberately uninstalled app, and compare the code returned. |
| 24 | **`estimatedMrr` is our arithmetic over Stripe's data, and Stripe computes the same figure differently and authoritatively.** The hourly snapshot sums `unit_amount x quantity` over `status: 'active'` subscriptions, dividing annual prices by twelve. Three known divergences travel on the wire in `excludes` — discounts (Stripe applies coupons and this does not, so ours reads HIGH), trials (`trialing` is excluded here while the tenant status mapping folds it into `ACTIVE`), and past-due. The arithmetic is tested against a mocked `subscriptions.list`, which pins the shape and not the premise: **nothing in this repository can compare the output to Stripe's own MRR**, which is the only comparison that would say whether those three are the only three. | `apps/auth-service/src/modules/finance/billing-snapshot.job.ts` (`compute`, `monthlyAmount`), `libs/common/src/configs/billing.config.ts` (`REVENUE_EXCLUSIONS`) | **Same shape as #22 and #23**: a premise that cannot be settled from inside the system. Bounded rather than open — the field is named `estimatedMrr` rather than `mrr`, a mixed-currency or unpriceable set refuses instead of producing a total, and `excludes` is on every response including the degraded ones, so a client cannot render the number without the caveat. *Closing it:* open Stripe's Billing dashboard for the same account on the same day and compare. If the two agree, narrow the caveats; if they do not, the difference names the fourth exclusion. **The honest answer may be a link rather than a better formula** — Stripe already computes this properly, and a figure that must be authoritative should come from where it is authoritative. |
| 25 | ***Closed.*** `provision-stripe.mjs` owns the endpoint: `STRIPE_WEBHOOK_URL` (root `.env`) → create with `enabled_events` **derived** from `STRIPE_ENTITLEMENT_EVENTS` + `STRIPE_DUNNING_EVENTS` in `billing.config.ts` — the same arrays `entitlement-writer.service.ts` narrows with `satisfies readonly Stripe.Event['type'][]`, so the list the endpoint sends and the list the writer handles are one list, typo-checked where the Stripe type exists. Matched by `metadata.synapsedesk_webhook`, never by URL. Drift in `enabled_events` is updated; drift in `api_version` is **reported and not repaired**, because `WebhookEndpointUpdateParams` has no such field — the fix is delete-and-recreate, which rotates the signing secret, and the script says so rather than pretending `up to date`. The secret is printed exactly once, at creation. The restricted key needs `Webhook Endpoints` **read and write** — measured: a dry run lists before it decides and fails on `webhook_read` without it. `observedEventTypes` stays as the runtime half. Original text follows. **`billing_events` is only as complete as an `enabled_events` list nothing in this repository sets.** Every figure in `GET /platform/finance/events` — new subscriptions, cancellations, failed payments — counts rows written when a webhook arrives. Which event types arrive at all is configured on the Stripe **endpoint**, and `scripts/provision-stripe.mjs` creates Products, Prices and the portal configuration and **never touches `webhookEndpoints`**. The endpoint is created by hand in the Dashboard, or by `stripe listen` in development, or not at all. | `scripts/provision-stripe.mjs`, `apps/auth-service/src/modules/billing/entitlement-writer.service.ts` (`claim`, which records EVERY verified event), `apps/auth-service/src/modules/finance/finance.service.ts` (`listEvents`) | **"No cancellations this month" and "cancellations were never enabled" render identically** — a zero on a chart, with nothing to distinguish them. That is the silent-zero failure `evaluated_dimensions` was added to prevent for plan applies, in a surface that had no equivalent. **Half-closed:** `observedEventTypes` reports the types actually seen in the range, so a type that has never arrived is visibly absent rather than reported as zero — a reader who looks can tell. What remains open is that nobody has to look. *Closing it:* extend `provision-stripe.mjs` to create the webhook endpoint with an explicit `enabled_events` list, putting the configuration in version control. Same class as #20, one endpoint further along: an input that decides what the system knows, living outside the repository. |
| 26 | ***Closed.*** **The SSRF deny-list is IP-range arithmetic in one file, not a vetted library, and it was the whole security boundary of outbound webhooks.** `addressIsDenied` refuses RFC1918, loopback, link-local, CGNAT, the IPv6 equivalents and IPv4-mapped forms — checked at connection time inside the socket's `lookup`, on every resolved address, with the connection pinned to a checked one so DNS rebinding has no second resolution to exploit. **It is no longer the only layer.** `k8s/policy/notification-egress.yaml` denies RFC1918, loopback, CGNAT and link-local egress from the notification pod — including `169.254.169.254`, the cloud metadata endpoint — which is the deployment artifact this row said the repository did not contain. A NetworkPolicy is allow-only, so the refusal is spelled as an `ipBlock` of `0.0.0.0/0` with those ranges in `except`. | `k8s/policy/notification-egress.yaml`, `apps/notification-service/src/modules/webhooks/webhook-target.guard.ts` (`addressIsDenied`, `buildGuardedLookup`), `webhook-sender.service.ts` | The guard stays, and the two layers answer different questions: the guard is the only one that can tell the tenant WHICH URL was refused and why, and the policy is the only one that holds when the guard is wrong — a novel encoding, an address class it does not enumerate, a resolver returning a form the parser does not expect. Two real holes were found in the guard after it shipped, which is the argument FOR the second layer rather than against the first. **What remains outside it**: the policy is per-pod and applies only where a CNI enforces NetworkPolicy — a cluster without one accepts the object and ignores it, silently, which is the same shape of failure the row was about. |
| 27 | ***Closed.*** `helmet` in `main.ts` **and** `test/utils/bootstrap.ts`, both reading one `SECURITY_HEADERS` object (`security-headers.config.ts`), with the CSP written against the one HTML page this gateway serves — Swagger UI: `script-src 'self'` with no nonce (the template's `customJs` slots render empty), `style-src 'unsafe-inline'` **because of the template's unconditional `<head>` reset, not the `customCss` slot** — the docblock says so because the obvious later tightening would break the page on the other block — `img-src data:` for `swagger-ui.css`, `frame-ancestors 'none'`. `/graphql` (`playground: false`) and the WebSocket upgrade never see it. The `security-headers` sweep asserts headers on a JSON route, that every `<script src>`/`<link href>` in the served docs page is same-origin, that the page has no inline script, and that both bootstraps share the config — **the last is the only test that fails when `main.ts` alone drops `helmet`**, because e2e boots `bootstrap.ts`; the CORS sweep's gap, one middleware over. There are no HTML error pages to secure; the exception filter is JSON. Original text follows. **No security-response headers at all — the ingress requirement's other half, found while closing its first.** There is no `helmet` and no `contentSecurityPolicy` anywhere in this repository: `main.ts` applies `cookieParser`, CORS, the exception filter, the logging and transform interceptors and the global `ValidationPipe`, and nothing sets `Content-Security-Policy`, `X-Frame-Options`, `Strict-Transport-Security`, `X-Content-Type-Options` or `Referrer-Policy`. `docs/tech-stack-spec.md` listed "strict Content Security Policy (CSP) headers, custom error pages" alongside reverse proxying and TLS as one row, and Phase 4 closed the proxying half — which is exactly how the other half becomes invisible. | `apps/api-gateway/src/main.ts`, `docs/tech-stack-spec.md` (§7, Ingress & Proxy), [ADR 0043](../decisions/0043-the-cluster-shape.md) | Mostly a JSON API, so the classic reflected-XSS vector is thin — but `sanitize-html` runs on stored user content precisely because that content is rendered somewhere, and the Swagger UI is a real HTML page this service serves. **The reason this is a row rather than a task**: `ingress-nginx` can also set these, through a `configuration-snippet` annotation, and that is the WRONG place — it is a second list to keep, one that does not version with the code deciding what a page may load and does not survive a change of ingress controller. The same "two lists is two chances" argument `secure-gateway.decorator.ts` makes about refusing a second `SOCKET_CORS`. *Closing it:* `helmet` in `main.ts`, with the CSP written against what Swagger UI actually loads. |
| 28 | **The bootstrap Super Admin's password is a Secret mounted forever to serve an action taken once.** `seedRows()` creates the account on the first boot against an empty database and skips it on every boot after — *"If the account already exists the .env password is ignored, so redeploying never resurrects a rotated or leaked credential"* — but `SUPER_ADMIN_PASSWORD` is `required()` and `hashSuperAdminPassword()` reads it UNCONDITIONALLY on every seeding boot, hashing a value it then discards. So the credential is in the `auth-service` Secret, mounted into every auth pod, permanently, for one row. The guard that matters is already in place — the schema refuses both published values outside `NODE_ENV=test` (`PUBLISHED_SUPER_ADMIN_PASSWORDS`) — and only the lifecycle is open. | `apps/auth-service/src/modules/prisma/database.seeder.ts` (`seedSuperAdmin`, `hashSuperAdminPassword`), `apps/auth-service/src/common/configs/env.validation.ts`, `k8s/generated/auth-service.secret.example.yaml` | Its exposure is the same as every other Secret in the namespace, which is the argument for leaving it — and it is the only Secret whose *purpose* expired after the first deploy, which is the argument against. **The fix is a one-shot Job**, `SEED_ON_BOOTSTRAP=false` on the Deployment and the Secret mounted only on the Job: the shape [ADR 0043](../decisions/0043-the-cluster-shape.md) gave the DDL, one step further, and the only option where the value is absent from serving pods. *What it costs, measured:* `seedRows()` has no standalone entrypoint — `schema-apply.ts` was written for the DDL half — so the Job needs a second entrypoint written the same way; and `SEED_ON_BOOTSTRAP` is now GENERATED into the ConfigMap from `.env.example`, so a per-workload override is something `scripts/generate-k8s-config.mjs` has no concept of. Neither is hard; both are more than "add a Job". Revisit once the cluster has served real traffic — stacking a second relocation before the first has run in anger is how a deployment path stops being understood. |
| 29 | ***Closed.*** The projection is **cursor-driven**: `project(until)` reads `projection_cursors` for its lower bound, runs the two `+=` statements over `[cursor, until)`, and upserts the cursor **last and inside the same transaction** — so every generation is counted by exactly one run, and the windowed overlap that added each one ~four times is gone. `until` lags `now` by `PROJECTION_LAG_MS`, the one subtlety a cursor has that an overlapping window did not: under READ COMMITTED a row committed a moment after the statement started would otherwise be missed forever. **It refuses to run without a cursor** (`ProjectionCursorMissingError`) rather than invent one, because seeding it means resetting every chunk counter, and an `UPDATE` over every `document_chunks` row locks the table `writeChunkRows` writes on every upload — not something a 02:00 job should do to a live system. **That makes this the one closed row with an operator step:** `npm run projection:backfill -w @synapsedesk/ingestion-service`, once per database, before the nightly step will run. It resets in `PROJECTION_RESET_BATCH` batches, projects in `PROJECTION_BACKFILL_WINDOW_DAYS` windows from `MIN(created_at)` — each committing its own cursor, so an interrupted backfill resumes and a completed one is idempotent. Documented beside the migration baseline step in `k8s/README.md` and in the root `README.md` setup. What the cursor does **not** fix: `step()` catches without rethrowing, so a failed projection still lets `document-flags` run — against counters that are now stale rather than wrong. Original text follows. **`chunk-usage-projection` accumulates on the normal daily path — no retry or failure involved.** It writes `SET retrieval_count = c.retrieval_count + usage.hits` (and the same for `citation_count`) over `trailingWindow(now, ROLLUP_TRAILING_DAYS)`, where `ROLLUP_TRAILING_DAYS` is 3 and `trailingWindow` subtracts `days + 1` — **a four-day window recomputed daily with nothing resetting the counters**, so each generation's hits are added by roughly four successive runs. **The codebase already draws the distinction one step later in the same sequence**: `AiGenerationRollupJob` uses delete-then-insert *"so a re-run recomputes rather than accumulates"*, because *"two rollups that behave differently under re-run is exactly the kind of difference nobody remembers when debugging a doubled number."* `trailingWindow`'s half-open defence covers **adjacent** windows; this one overlaps deliberately, and its justification (*"a window an hour too wide re-computes a day that was already correct"*) is true for delete-then-insert and false for `+=`. | `apps/ingestion-service/src/modules/scheduled/chunk-usage.projection.ts`, `libs/common/src/utils/rollup-window.ts` (`trailingWindow`), `libs/common/src/configs/scheduler.config.ts` (`ROLLUP_TRAILING_DAYS`), `apps/ingestion-service/src/modules/analytics/ai-generation-rollup.job.ts` (the contrasting shape) | Bucketing is unaffected — `neverRetrieved` and `retrievedNeverCited` classify on zero thresholds, as do the document flags. The **absolute counts** in `analyticsDocuments` are inflated ~4x, and **`mostCited` is systematically biased against recent documents**: one cited yesterday has had one pass, one cited last week has had four. A user reads that as a finding about their content. *Closing it:* a **watermark**, not a window — project `[last_projected_at, now)` and advance a cursor in the same `$transaction` as the two writes, so no row is ever in two intervals and `+=` becomes correct. The read (`ai-analytics.service.ts`) has **no date predicate** and `analyticsDocuments(limit)` takes no `from`/`to`, so a per-day rollup table would buy this consumer nothing and would be a behaviour change: `writeChunkRows` does `deleteMany` then `createMany`, so today's counter is really *lifetime since last reindex*. **`job_runs.lastSucceededAt` cannot be the cursor** — it is `@id` on `jobName`, not per step, and the sequence stops when a later step throws, so a successful projection followed by a failed `document-flags` never advances it and the next run re-projects the same interval. |
| 30 | ***Closed.*** **The inbound-email Worker retried a 401, which is the one status it must not.** Both branches in `workers/email-inbound/src/index.ts` ended in `throw`, and an uncaught throw out of `email()` is what makes Cloudflare retry — so a wrong `INBOUND_SECRET` produced an unbounded retry of every inbound message, the flood the design wanted to avoid. The claim that it did not retry existed in two places, `index.ts` and `wrangler.toml`. **The 401 branch now returns**, every other non-2xx still throws (a 5xx is transient and the `(organization_id, message_id)` constraint makes that retry safe), and both comments say what the code does. | `workers/email-inbound/src/index.ts`, `workers/email-inbound/wrangler.toml`, `apps/api-gateway/src/common/guards/inbound-signature.guard.ts`, `apps/api-gateway/src/modules/metrics/metrics.registry.ts` | Returning alone would have replaced a storm with silence: this Worker has no `observability` block, no tail consumer and no logpush, so the throw was its only reporting mechanism. The durable record is now a gateway counter — `inbound_email_webhook_total`, incremented at all three of the guard's exits — where `rejected_signature` means the secret is wrong and `accepted` falling to zero means the Worker is not calling at all, a failure invisible in the first series. **What is still open is the reader**: nothing scrapes `/metrics`, so a wrong secret pages nobody and is visible only by reading port 9464 by hand. That is tracked as **row 31** rather than here — the counter, the drop and the comments are done. Verified by `inbound-signature.guard.spec.ts` (three labels) and `workers/email-inbound/src/index.test.ts` (a 401 resolves, a 500 still rejects). |
| 31 | ***Closed for development; production is a decision, not a gap.*** `docker-compose.yml` carries a `prom/prometheus` behind the **`observability` profile** — the first profile in that file — mounting `docker/prometheus/` read-only so the generated `job-alerts.yml` and the hand-written `prometheus.yml` arrive together. Opt-in on purpose: in the default set CI would pull and start a collector no CI job scrapes. **Two setup facts, both measured in both directions on a real machine:** the gateway's listener defaults to `127.0.0.1` (deliberately — the closed default), so a container scraping the host finds nothing and the target reads `DOWN` until `METRICS_HOST = 0.0.0.0` is set in `apps/api-gateway/.env`; and `host.docker.internal` needs an explicit `extra_hosts: host-gateway` on Linux. **The profile's teardown trap:** a bare `docker compose down` leaves the profiled container running and holding the network, prints `Resource is still in use`, and **exits 0** — `--profile observability down` is the teardown, recorded where the profile is introduced. **The acceptance that matters is a rule clearing, not a page loading:** `/rules` must show 18, because a `rule_files` path matching nothing loads zero rules and logs *success* at INFO — no warning exists — and `ScheduledJobMissing` is `absent(...)`, which fires identically for a DOWN target and a healthy one with no heartbeat, so only watching it **clear** after a real job run proves the chain. **`k8s/` deliberately has none:** production collection is a question about retention, alert routing and an on-call destination — the managed-versus-in-cluster line [ADR 0043](../decisions/0043-the-cluster-shape.md) already drew for Postgres and Redis — and choosing a vendor with no operator to route to is a guess, not a decision. It gets its own ADR when there is one. Original text follows. **Nothing scrapes `/metrics`, so every alert in this repository is a rule with no reader.** The gateway exports on `METRICS_PORT` (9464) — `job_last_success_timestamp_seconds` per scheduled job through `job-metrics.collector.ts`, `inbound_email_webhook_total` at the inbound-signature guard's three exits — and `docker/prometheus/job-alerts.yml` is **generated** from `SCHEDULED_JOBS` with a staleness threshold per job's own cadence. There is no Prometheus in `docker-compose.yml`, none in `k8s/` (the manifests only make scraping *possible*: 9464 on its own listener, `METRICS_HOST=0.0.0.0`), and none in CI. `prom-client` lives only in the gateway. | `apps/api-gateway/src/modules/metrics/`, `docker/prometheus/job-alerts.yml`, `scripts/generate-job-alerts.mjs`, `k8s/README.md` (*What is deliberately not here*) | A stopped nightly job, a wrong `INBOUND_SECRET`, a scheduler that lost its repeat entry on a Redis flush — each is a metric that goes flat and pages nobody; the signal exists and is visible only by reading port 9464 by hand. Rows 16 and 30 both stop at this row. *Closing it:* a Prometheus (compose service for development; in-cluster or managed for production) with one scrape target and `job-alerts.yml` mounted as its rules file, plus an alert receiver — and `scheduling.md`'s *"nothing scrapes it yet"* goes. The generator's `--check` mode already keeps the rules file honest; what is missing is the reader, not the rules. |
| 32 | **Ten contract guards read a file list that can change under them.** Each builds its corpus from `git ls-files --cached --others --exclude-standard` and then reads every path with a bare `readFileSync`. `--others` includes **untracked** files, so a scratch file, a probe, a build artifact or an editor temp that exists at `ls-files` and is gone at `read` throws `ENOENT` from inside the helper rather than failing an assertion. **One sighting**, in `flow-references.spec.ts` `declaredConstants()` during a session that was creating and deleting docker probe files alongside the suite; not reproduced in nine subsequent runs. Staging a new file removes it from the `--others` half of the corpus, which is why the failure is transient and why it will recur rather than stay fixed. | `libs/common/src/configs/` — `flow-references`, `jsdoc-tags`, `env-contract`, `typecheck-coverage`, `archive-references`, `manifest-contract`, `image-contract`, `ci-contract`, `schema-contract`, `mapper-naming` | A red suite nobody can reproduce, in the guards whose whole purpose is to be trustworthy — the failure names a line inside the helper, not the corpus that moved, so the reader looks at the assertion rather than at the race. Same family as #7 and #13 and a different cause from both: not shared state, not a poll budget, but **a corpus enumerated in one process and read in another moment**. *Closing it:* filter the corpus through `existsSync` once, or catch `ENOENT` per read and drop the path — and say in the comment that `--others` is why, since a reader who does not know the corpus includes untracked files will read the guard as pointless. The `--others` half is itself deliberate: it is what makes a guard cover a file before it is committed. |
| 33 | **The chat auto-reply books `purpose=DRAFT`, so it lives inside the co-pilot acceptance rate as a permanent `DISCARDED`.** The auto-reply in `messages.service.ts` calls rag's `Draft`, which records `AiGenerationPurpose.DRAFT` — the same purpose an agent's co-pilot draft records — and then never calls `recordOutcome`, so the hourly `discarded-draft` sweep marks every auto-reply `DISCARDED`. The obvious fix — record `ACCEPTED`, since the sent text always equals the draft — is the mirror image: every auto-reply becomes an acceptance and the rate is pushed toward 100%, which is precisely the failure the sweep's own docblock exists to prevent. Found while planning citation persistence, where a *"fix in passing"* would have shipped the mirror image. | `apps/ticket-service/src/modules/messages/messages.service.ts` (the auto-reply block), `apps/rag-service/rag_service/server.py` (`Draft` books `DRAFT`), `apps/ingestion-service/src/modules/scheduled/discarded-draft.sweep.ts` | The one number justifying the co-pilot is diluted by rows that were never drafts for anyone: an auto-reply is a customer-facing answer, not a suggestion an agent accepted or rejected. Its `resulting_message_id` also stays NULL, so the ledger cannot find the message it produced. *Closing it:* a purpose of the auto-reply's own — `AUTO_REPLY` in `AiGenerationPurpose`, booked by rag when the caller says so (a request flag, the way `triggered_by_escalation` distinguishes the two summaries), excluded from the acceptance rate by construction, and linked to its message with `recordOutcome` without pretending an acceptance happened. A rag-service change plus the enum bridge, so it is its own batch. |
| 34 | **The HTTP metrics interceptor labels a handler error as `status="200"`.** `http-metrics.interceptor.ts` reads `response.statusCode` inside `tap`, which runs before the exception filter has set the status — so a `400` thrown from a handler is recorded as a `200`. Measured while validating the versioning plan: a request that returned 400 to the client incremented the 200 bucket. | `apps/api-gateway/src/modules/metrics/http-metrics.interceptor.ts` (`tap`, `status: String(response.statusCode)`), `apps/api-gateway/src/common/filters/` (the filter that sets the status) | Every error-rate dashboard built on this metric reads near zero: the failures it exists to count are filed under success. *Closing it:* read the status in the interceptor's `error` arm from the exception (`HttpException.getStatus()`, `500` otherwise) rather than from the response, or move the observation to a `finish` listener on the response, which fires after the filter. A unit test that throws a `BadRequestException` through the interceptor and asserts the `400` label is the proof. |
