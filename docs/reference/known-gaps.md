# Known Gaps — Read Before You Trip Over Them

**Current state.** Real, current, and they bite silently. Each row names the file that proves it. And when a gap is closed, just add ***Closed.*** to indicate it's closed.

| # | Gap | Where | Consequence |
| :---- | :---- | :---- | :---- |
| 1 | **Tenant scoping is applied by hand.** `tenantScope()` is the shared helper and is used across services, but there is **no Prisma middleware or `$extends`** — nothing forces a query to call it. | `libs/common/src/utils/tenant-scope.ts` | A forgotten `...tenantScope(ctx)` is a cross-tenant data leak, not a bug. **`findUnique({ where: { id } })` cannot express the filter at all** — always `findFirst` + scope, and let a miss be `NOT_FOUND`, never `PERMISSION_DENIED`. |
| 2 | **`otps.code_hash` and `two_factor_backup_codes.code_hash` use SHA-256** where a slow KDF is the better fit (§8.1). Both columns are already `VarChar(255)` to accommodate one. | `apps/auth-service/src/modules/otp/otp.service.ts` (`hashToken(code)`), `apps/auth-service/src/common/utils/crypto.ts` | A 6-digit code is ~20 bits — offline-guessable if the table leaks. `maxAttempts` + short expiry are the online defence today. |
| 3 | ***Closed.*** Deferred ingestion jobs are re-queued by `IngestionReconcileSweep` (`ingestion-reconcile`, every ten minutes), which also recovers the case this row never named: a `document.uploaded` lost to core NATS, where the job row sits `QUEUED` with `bullmqJobId: ''` and nothing was ever enqueued. The number is kept rather than reused so citations of `#4` and above stay valid. | — | — |
| 4 | ***Closed.*** At most one live ingestion job per document is now enforced by `ingestion_jobs_one_live_per_document`. The number is kept rather than reused so citations of `#5` and above stay valid. | — | — |
| 5 | **The `users` super-admin CHECK constraint was never applied.** `apps/auth-service/prisma/schema.prisma:151` says `organization_id` is *"kept in lockstep with isSuperAdmin by a DB CHECK constraint added via migration: (organization_id IS NULL) = is_super_admin"*, There are no migrations, and the seeder's DDL block creates exactly one CHECK on `users` — `users_locked_until_requires_lock` — which is a different constraint. `development-conventions.md` §7 used to name this constraint as one of two exceptions, which is how a constraint that does not exist was documented as existing for as long as it was; §7 now points at the seeder blocks instead (ADR 0039), so the block is the only place to read its absence. | `apps/auth-service/prisma/schema.prisma:151`, `apps/auth-service/src/modules/prisma/database.seeder.ts` (the raw-DDL block), `docs/development-conventions.md` §7 | Nothing prevents `organization_id = NULL` with `is_super_admin = false`: a row with no tenant that is not a platform account, which every tenant filter treats as unreachable rather than as an error. Any code reasoning "super admins have no tenant" — e.g. the `!== undefined` guard in `AuditPublisher.record` — is relying on convention, not on the database. |
| 6 | **`?search=` is advertised on five list routes and honoured on none of them.** `SearchPaginationDto` puts `searchTerm` on the wire and into Swagger, but `toSearchFilter(page.searchTerm)` is called in exactly two services — `documents.service.ts:314` and `tickets.service.ts:120`. The DTOs for ingestion jobs, document flags, audit logs, ticket messages and feedback all extend it and all ignore it. | `apps/api-gateway/src/common/dto/rest/search-pagination.dto.ts` (the base), and the five `List*QueryDto` classes extending it | A caller filters a worklist by `?search=`, gets an unfiltered page back, and reads it as "no other matches exist". Silently dropping a documented filter answers a different question than the one asked — the failure `listDocumentFlags` explicitly refuses an unknown flag type to avoid. |
| 7 | **`scheduler-ownership.e2e-spec` fails intermittently on a full auth-service run.** Three sightings across three docs, each on `npm run test:e2e:auth` with every other suite in the run, each passing in isolation on the next attempt, and each with the session's changes nowhere near a scheduler. The suite asserts BullMQ queue isolation against a **shared real Redis**, and every service's e2e suite uses the same instance — `bootstrap.ts` already documents the class of problem for ingestion ("WORK must not escape its test"). | `apps/auth-service/test/modules/scheduler-ownership.e2e-spec.ts`, `apps/auth-service/test/utils/bootstrap.ts` | A red suite that is green on re-run teaches the team to re-run. **FOURTH SIGHTING, CAPTURED (doc 49).** Failure: the ticket assertion, with `analytics-daily` seen TWICE — one job processed twice, not a missing one. Redis at that moment held live repeat schedulers left by OTHER suites' app bootstraps: `bull:scheduler-ticket:repeat:repeat-analytics-daily` (+1 delayed job) and `bull:scheduler-auth:repeat:repeat-auth-{hourly,daily}` (+2 delayed). Those are written by each service's `SchedulerRegistrar` at bootstrap and **nothing removes them at teardown**, so they outlive the suite that created them in the shared Redis. The duplicate carries the name of a leftover repeat entry, on the queue that leftover sits on. The spec itself is not the leak — run in isolation it passes AND obliterates all three scheduler queues in `afterEach`, which is why the leftovers came from elsewhere. **Not proven end to end:** the delayed jobs' next-fire times were in the future at capture, so promotion into the test window is the step still unshown. **Likely fix:** each service's e2e teardown obliterates its own `SCHEDULER_QUEUE`, so one suite's registrar cannot leave a live schedule for another suite's worker. **SIXTH SIGHTING (doc 63), CAPTURED, AND THE UNSHOWN STEP IS NOW SHOWN.** Same spec, same test, same signature — `analytics-daily` seen TWICE on `scheduler-ticket` during a full `test:e2e:auth`. Redis was captured before any re-run this time. What it held: `bull:scheduler-auth:repeat:repeat-auth-{hourly,daily}` plus their delayed jobs, with `repeat-auth-hourly` next-firing at `06:00:00.000Z` — and at `06:00:11Z`, eleven seconds PAST that time, the delayed job was still sitting there unprocessed. **Nothing promotes a leftover in the background, because no worker is alive between runs.** Probe: with the stale `…:1787983200000` job present, an unrelated suite (`platform.e2e`) was run; afterwards that job's hash had grown from 7 fields to 12 (`processedOn`, `finishedOn`, a return value) and the schedule had advanced to `07:00` with a fresh delayed job. So promotion is **bootstrap-driven, not time-driven**: any leftover whose fire time has passed is promoted and PROCESSED by the next suite that boots a worker on that queue, whichever suite that happens to be. That is the missing link — it explains why the failure needs a full sequential run (many bootstraps, and long enough to cross a fire boundary) and why an isolated re-run is green (a fresh obliterate and a short window). The fix shape is unchanged and now has a reason rather than a hunch: teardown must obliterate its own `SCHEDULER_QUEUE` because it is leaving behind a schedule that another suite will EXECUTE, not merely a key that another suite will read. **SEVENTH SIGHTING (doc 65), not re-captured deliberately.** Same spec, same test, on a full `test:e2e:auth` whose changes were confined to notification-service. The mechanism is settled — the sixth sighting demonstrated bootstrap-driven promotion end to end — so a further capture would add a date and nothing else. What this sighting adds is frequency: it now appears on roughly every second full auth run, which is the argument for doing the teardown fix rather than continuing to log it. **FIFTH SIGHTING (doc 58), and the capture instruction was not followed.** Same spec, same test — *"each service receives its OWN jobs and none of its neighbours'"* — failing on a full `test:e2e:auth` inside a seven-suite sequential sweep, passing on its own immediately after. What was NOT captured is the thing this row asks for: Redis state at the moment of failure. It was re-run before being inspected, which is precisely the reflex the row exists to break, so this sighting adds a date and nothing else. **The instruction needs to be actionable to be followed:** "capture before re-running" competes with a sweep that has already moved on to the next service by the time anyone reads the output. A `--runInBand` sweep that dumps `KEYS bull:scheduler-*` into the log on failure would collect it without anyone remembering to. |
| 8 | **Three writes move `roles.user_assigned` without evicting the cache that shows it.** `GET /roles` caches `userAssigned` for five minutes per tenant. `PUT /users/:id/roles` and `DELETE /users/:id` now evict it; **`POST /invitations/accept` cannot** — `CacheInvalidationInterceptor` derives the tenant from the request context and returns early without one, and that route is unauthenticated by design. | `apps/api-gateway/src/modules/invitations/invitations.controller.ts` (`accept`), `apps/api-gateway/src/common/interceptors/cache-invalidation.interceptor.ts` | After an invitation is accepted, the roles screen shows a stale `userAssigned` for up to five minutes, and a cached hit is indistinguishable from a fresh read. Covered by TTL, which is the same trade that interceptor already accepts for a platform Super Admin writing into a tenant. Closing it needs an eviction path that does not depend on a caller — the NATS consumer that class mentions. |
| 9 | **The export family is named for one of its four kinds.** `AnalyticsExport`, `analytics_exports`, `AnalyticsExportKind`, `ANALYTICS_EXPORT_QUEUE` and `apps/ticket-service/src/modules/analytics/analytics-export.*` now carry `TICKET` and `AUDIT_LOG` exports, which read live tables and are not analytics. The shared thing is the LIFECYCLE — one status machine, one queue, one processor, one poll route — which is why they share a table rather than getting two more. | `libs/common/src/configs/analytics.config.ts` (`AnalyticsExportKind`), `apps/ticket-service/prisma/schema.prisma` (`AnalyticsExport`) | A reader looking for the ticket export finds it under analytics, and a table called `analytics_exports` holds audit-log rows. Renaming to `Export` is ~18 files outside generated code plus a proto regeneration and the enum-bridge registry — deferred deliberately, not overlooked. Do it as its own change, or the half-done state sits under three features. |
| 10 | **Nothing reports which roles still hold a RETIRED permission code.** A code removed from `PERMISSION_CODES` is deliberately left in the `permissions` table, because tenant custom roles may still reference it and dropping the row would cascade that grant away silently (ADR 0038). `SeedSummary.retiredPermissionCodes` names the CODES at boot; nothing names the roles. | `apps/auth-service/src/modules/prisma/database.seeder.ts` (`seedPermissions`), `apps/auth-service/src/modules/roles/roles.service.ts` (`listPermissions`) | After a retirement an operator can see that a code is dead and not who is still carrying it — so a role keeps advertising a capability no guard will ever satisfy, and nobody is told which roles to edit. The query is one line: `role.findMany({ where: { permissions: { some: { code: { in: retired } } } } })`. Latent until the first removal — the table and the array match exactly today. |
| 11 | **A re-run re-embeds the whole document and is billed for it.** Every entry into `process()` runs `writeChunkRows`, which deletes and recreates every chunk row, so the `vector_point_id IS NULL` filter in `embedAndUpsert` matches the entire document and no embedding work carries across runs. A retry after a failure in the last batch of a 400-page handbook re-embeds all 400 pages, metered as `EMBEDDING` against the tenant's budget. | `apps/ingestion-service/src/modules/ingestion/ingestion.processor.ts` (`writeChunkRows`, `embedAndUpsert`) | The tenant pays twice for one document, and the charge is invisible — it looks like ordinary ingestion spend. Distinct from #3, which is about a deferred job never being re-enqueued at all; this is about what a run that DOES happen redoes. Resuming across runs needs the point ids to survive `writeChunkRows`, which is a different shape of pipeline, so `ingestion-pipeline.e2e-spec` test 19 pins the current cost rather than asserting it away. |
| 12 | **`ticket_assignments.assigned_by_id` is nullable for a case that cannot occur.** The column says *"null when the system assigned it"*, but `writeAssignment` is its only writer and takes `requireActor(context)` — and `load` refuses a caller with no identity, so no assignment can happen without an actor. Same shape as `ticket_status_changes.changed_by_id`, which was made `NOT NULL` for exactly this reason when it was added. | `apps/ticket-service/prisma/schema.prisma` (`TicketAssignment`), `assignments.service.ts` (`writeAssignment`), `assignment.proto:16` (`optional string assigned_by_id`) | A reader takes the null as a supported case and writes a branch for it — `assignment.mapper.ts` already has one (`?? undefined`). Closing it is a four-file chain: schema, proto, mapper, gateway DTO. Deliberately not done inside doc 47, which is about ticket reads and the status history; the comment was corrected in place so it no longer misleads. |
| 13 | **Two specs fail intermittently on full runs because their ARRANGEMENT assumes an idle machine — and neither is gap #7.** `ingestion-pipeline` 6c and `realtime` Presence 5. Both fail only on a full run and that is where the resemblance stops. #7 asserts BullMQ queue isolation against a **shared real Redis** — genuine cross-suite interference. 6c hangs a `downloadObject` promise it owns and then `waitFor`-polls **its own job row in Postgres** until the status reads `PARSING`; it touches no queue and opens no Redis connection. Its failure is a **poll budget sized for an idle process**, expiring when `--runInBand` gives the box more to do. One sighting each, in two services. | `apps/ingestion-service/test/modules/ingestion-pipeline.e2e-spec.ts` (6c), `apps/api-gateway/test/modules/realtime.e2e-spec.ts` (Presence 5), `realtime.gateway.ts` (`announceConnected`), `@synapsedesk/common/testing/wait` | Filed separately on purpose: merging it into #7 would point the next person at Redis for a test that never opens a connection to it, and merging two rows later is cheap while un-merging a generalization that has already sent somebody to the wrong system is not. **SECOND SIGHTING, CAPTURED (doc 49 validation), different spec, mechanism identified.** `realtime.e2e-spec` Presence 5 expected `away` and got `online`. `announceConnected` broadcasts `online` to `org:{id}` whenever a connecting client's state CHANGES, and the test connects three clients before registering its listener — so `colleague` receives its own connect-time `online`. On an idle machine those broadcasts land before `waitForEvent` registers; under a full run they can land after, and the listener resolves with the wrong event. Same class as 6c: **a test whose arrangement assumes the machine is idle**, not cross-suite interference — no shared state is involved in either. **Fix shape (both):** make the arrangement wait for the thing it is racing rather than assume it has already happened — drain connect-time presence before listening; widen or remove 6c's poll budget. **Next sighting, capture before re-running:** the elapsed time at failure and what else was running. Note #7's capture instruction is scoped to one spec, which is probably the bug — the information it protects (which suites were in the run) is lost either way. |
| 14 | **A suite that tears down shared broker state can fail an unrelated service's suite mid-run — a third distinct class from #7 and #13.** Caught during doc 50, not in the wild: `jetstream-bootstrap.e2e-spec` deleted the real `AUDIT` and `NOTIFICATIONS` streams in `afterAll`, and ticket-service's audit suite failed with `NatsError: stream not found` on every test. Fixed at the source — that spec now deletes nothing and relies on `ensureStream` being idempotent — so **the row is here for the class, not for a live defect**. The same shape is available to any suite that mutates the one broker, the one Redis or the one Qdrant: #7 is *leftover state* another suite reads, #13 is *timing* on a busy machine, and this is *deliberate teardown* of something shared. | `apps/notification-service/test/modules/jetstream-bootstrap.e2e-spec.ts` (`afterAll`), `libs/common/src/utils/jetstream-bootstrap.ts` | The failure names the victim, never the culprit — the error surfaces in a service whose code did not change, in a run that may not even include the suite that deleted the stream. Filed separately for the reason #13 was: pointing the next person at "leftover schedulers in Redis" for a stream somebody dropped costs more than a fourth row. **Rule of thumb for new suites against shared infrastructure: create idempotently, delete only what you named uniquely.** **SECOND SIGHTING, SAME RUN, OPPOSITE DIRECTION.** `audit-consumer.e2e-spec` did not delete enough: a durable consumer's name is stable by design, so the suite inherited seven unacked messages from an earlier run and spent its first tests draining a backlog while asserting `count() === 1`. Two tests failed on the full run and passed in isolation — the signature of this whole family. Both halves of the rule are load-bearing: leaving shared state alone is not the same as leaving YOUR state behind. **THIRD SIGHTING, AND THE ONE THE RULE CANNOT FIX.** A durable left by a real `ticket-service` RUN — not by any suite — held `audit.record`, and a WorkQueue stream allows exactly one consumer per filter subject, so the whole audit suite failed with `filtered consumer not unique on workqueue stream`. "Delete only what you named" is no defence here: the obstacle belongs to nobody's suite, survives every restart because JetStream persists to disk, and the server's message names neither the subject nor the consumer holding it. Resolved in two places rather than by a rule: `PullConsumerRunner` now asks the server WHICH consumer holds the filter and says so, and `npm run nats:reset` drops the durable streams with their consumers and backlogs. Reach for the reset whenever a broker-backed suite fails wholesale rather than in one test. |
| 15 | **Nothing bounds the ingestion worker's memory, and there is no artifact in this repo that could.** The parser holds the whole file as a `Buffer` (`document-parser.service.ts`) at `concurrency: 2` (`ingestion.worker.ts:21`), so two documents plus whatever the parse library allocates are resident at once. `MAX_DOCUMENT_BYTES` is now **100 MB** (doc 53 §C), which makes that ceiling matter rather than creating the gap — it was unbounded at 25 MB too. Doc 53 §C3 called for "an explicit memory limit on the ingestion container": **there is no ingestion container.** `docker-compose.yml` holds infrastructure only — postgres ×4, redis, nats, qdrant, firebase-storage — with no `mem_limit`, no `deploy.resources` and no application service; there is no `Dockerfile` anywhere in the tree, and `apps/ingestion-service` starts with a bare `nest start --watch`. | `docker-compose.yml`, `apps/ingestion-service/package.json` (`dev`), `apps/ingestion-service/src/modules/ingestion/ingestion.worker.ts:21`, `libs/common/src/configs/document.config.ts` (`MAX_DOCUMENT_BYTES`) | "Does a 100 MB document OOM the worker" has no answer that does not begin with "depends which host it lands on" — and the failure mode without a limit is not a clean OOM-kill with a requeueable job, it is GC thrash that presents as the pipeline being slow. **What closes it is a deployment artifact this repo does not contain**, so it cannot be fixed here: whoever adds the Dockerfile or k8s manifest sets `--max-old-space-size` (or a container memory limit) sized against `MAX_DOCUMENT_BYTES × concurrency` plus parser overhead. **The chunk ceiling that shipped alongside is the half that COULD land here** — `MAX_CHUNKS_PER_DOCUMENT` bounds the embedding spend, which was the other unbounded cost, and it is enforced before any embedding call. |
| 16 | **OCR availability is a tenant-visible failure and an operator-invisible one.** `OcrService` probes `pdftoppm` and `tesseract` once at boot and, when either is absent, warns once and keeps serving — the run-open rule ADR 0016 argues for. Doc 55 closed the tenant half: a fully scanned document now refuses with `OcrUnavailable` naming the deployment, a partly scanned one flags the pages it lost, and both are logged at `error` per document rather than only once at boot. **The operator half has no surface to close it against.** There is no health module in `apps/ingestion-service` — nothing to add an indicator to, and gating readiness on a minority capability is the boot-closed behaviour the ADR rejects. There is no metrics surface either: `prom-client` lives only in api-gateway, and `job_last_success_timestamp_seconds` reaches it through `job-metrics.collector.ts` reading the `job_runs` heartbeat table other services write. | `apps/ingestion-service/src/modules/ingestion/ocr.service.ts` (`checkAvailability`), `apps/api-gateway/src/modules/metrics/job-metrics.collector.ts`, `docs/decisions/0016-ocr-is-a-per-page-branch.md` | A deployment built without `--target runtime-ocr` serves every text document correctly and fails every scanned one, and nothing an operator watches goes red — the signal is a boot warning that has scrolled away and per-document `error` lines that only appear once tenants are already affected. **Same shape as #15: the fix's target does not exist in this repo.** An `ocr_available` gauge would follow the `job_runs` pattern — ingestion writes capability state to a table, the gateway's collector exports it — which is a capability-reporting channel rather than a metric, and a larger change than the failure it reports. |
| 17 | **Nothing bounds the DECOMPRESSED size of an office attachment, and `MAX_ATTACHMENT_BYTES` cannot.** `.docx` and `.xlsx` are zip containers, so the 10 MB ceiling bounds the COMPRESSED upload while the decompressed object graph is unbounded — and `MAX_EXTRACTED_TEXT_CHARS` caps the RESULT of the parse rather than the parse itself. **This arrived with `.docx` in doc 56, not with `.xlsx` in doc 57**, and mammoth is the exposed half: `convertToHtml({ buffer })` reads the whole document into memory before anything is truncated. **`.xlsx` was supposed to be the safer of the two and is not.** Doc 57 chose exceljs for a streaming reader whose early exit would have bounded memory by `MAX_SHEET_ROWS` rather than by the file; measured against 4.4.0, `WorkbookReader` crashes on multi-sheet workbooks (103 failures in 200 runs on three sheets, 0 in 60 on one) at `workbook-reader.js:303`, under every option it accepts. `workbook.xlsx.load()` is reliable (200/200) and materialises the whole workbook, so the row cap is a post-hoc slice. Adding the second format did not widen this gap — both formats now load whole — but it did not narrow it either. | `apps/ingestion-service/src/modules/ingestion/document-parser.service.ts` (`parseDocx`, `parseXlsx`), `libs/common/src/configs/ticket.config.ts` (`MAX_ATTACHMENT_BYTES`, `MAX_EXTRACTED_TEXT_CHARS`) | A 10 MB zip of highly compressible XML expands to far more inside a worker with no memory limit, and the failure is not a clean OOM with a requeueable job — it is GC thrash that presents as the pipeline being slow. **Same shape as #15 and #16: the fix's target does not exist in this repo.** A decompressed-byte ceiling has nowhere to live in `DocumentParserService` today and would touch the ingestion document path as well, so it belongs with whoever gives the worker a memory limit — the same missing deployment artifact. Bounded in practice by `MAX_ATTACHMENTS_PER_MESSAGE`, `concurrency: 2`, and the fact that both parsers are only reachable through an authenticated upload. |
| 18 | ***Closed.*** The platform usage surface exists: `IngestionPlatformService.GetPlatformUsage` answers bytes AND document count for many tenants in one query, zero-filled per requested id, and the GATEWAY composes it with auth's seat projection — auth could not, because `ingestion-service` dials auth on every presign and the reverse edge would close a cycle on the identity leaf. `evaluated_dimensions` became dynamic in the process: a leg that does not answer drops its dimensions from that run rather than reporting nobody affected. **A GATE now reuses the same composition with the OPPOSITE failure policy.** `POST /billing/plan` folds the same two legs and refuses with `503` when the usage leg does not answer, because a block that fails open is not a block — degradation is the house rule for the REPORT and must not be copied into the gate. Both sides carry a comment saying so, since they read as if they should agree. The number is kept rather than reused so citations of `#19` and above stay valid. Original text follows. **The plan-apply dry run reports SEAT over-limit and nothing else — storage is invisible to it.** D1's headline example is *"this puts 12 of 47 subscribers over their storage limit"*, and the projection cannot answer it. `PlanAdminService.overLimit` counts active users plus pending invitations, both `auth-service`'s own tables. Storage used is `ingestion-service`'s to count and is exposed only as `GetStorageUsage`, which is **tenant-scoped from the caller context and calls back into auth for the limit** — so answering it for 47 subscribers would be 47 calls each triggering another, from a caller that has no tenant context to offer. | `apps/auth-service/src/modules/billing/plan-admin.service.ts` (`overLimit`), `libs/grpc-proto/src/proto/synapsedesk/ingestion/document.proto` (`GetStorageUsage`) | **The silent-zero half is CLOSED**: `ApplyPlanResponse.evaluated_dimensions` names the limits a run actually checked (`["seats"]`), so a zero `over_limit_count` can no longer be read as "nobody is affected" for a dimension nothing looked at. What remains open is the check itself. **Not closed by widening `GetStorageUsage`**: a cross-tenant read added beside tenant-scoped RPCs is precisely the leak shape `platform.proto`'s own header warns about — *"one forgotten filter away from leaking"* — so it wants a separated platform surface in `ingestion-service` mirroring the one `auth-service` has, plus a gateway that merges the two projections. Nothing is silently wrong meanwhile: seats are reported accurately, the coverage list says storage was not evaluated, and D1's rule means an over-storage tenant is refused their next upload rather than losing anything. |
| 19 | **Domain-matched registration takes a seat and checks nothing.** The other two paths that consume a seat — `invitations.create` and `users.create` — both compare `seatsInUse` against `maxAgentSeats`, refuse with `RESOURCE_EXHAUSTED`, and fire the seat alarm afterwards. `register` does neither: a tenant that lists its own domain in `allowedEmailDomains` can be taken past `maxAgentSeats` by self-registration alone. | `apps/auth-service/src/modules/auth/auth.service.ts` (`register`), `apps/auth-service/src/modules/organizations/organizations.service.ts` (`seatsInUse`, `alertOnSeats`) | The usage page reports `used > limit` with no event that produced it, and the tenant is never warned on the way there — the alarm's crossing is skipped along with the refusal. **Closing it is a refusal, and a refusal needs a product answer first:** the user is told their workspace exists and will not let them in, which is a worse experience than the overrun for a tenant one seat over. Not a code decision — that is why it is a row rather than a TODO. |
| 20 | **The plan-change block's integrity depends on a Stripe Dashboard setting nothing in this repo verifies.** `subscription_update.enabled = false` is written by `scripts/provision-stripe.mjs` at provisioning time and read back by nothing — and the script itself only WARNS when a configuration already exists, leaving it untouched. If the flag is flipped in the Dashboard, or a second portal configuration is created without it, a tenant changes plan inside Stripe's portal, the webhook arrives, `EntitlementWriterService` applies the new grants, and **`POST /billing/plan`'s over-limit block is bypassed with no error anywhere.** | `scripts/provision-stripe.mjs` (the portal configuration), `apps/auth-service/src/modules/billing/billing.service.ts` (`createPortalSession`) | The one enforcement point for "a downgrade is refused when current usage does not fit the target plan" sits outside this repo's control, and its failure is silent and indistinguishable from a plan change the endpoint allowed — the tenant simply lands over a limit. **This is the price of departing from Stripe's own advice**, which is to use the Customer Portal for self-service plan changes; the block cannot run inside Stripe's UI, because a change made there reaches this system only AFTER Stripe has applied it. *Closing it:* read the portal configuration in `createPortalSession` and refuse — or log loudly — when `subscription_update.enabled` is true. |
| 21 | **The plan-apply dry run parses a DISPLAY STRING, and a cosmetic edit silently disarms it.** `enrichWithUsage` reads the `after` half of `"before -> after"` out of the projection DTO and treats a failed parse as "this column is not changing", which means "no check". The separator is a contract between two services — `plan-admin.service.ts` writes it, the gateway's composer reads it — and nothing typed it. Measured: changing `" -> "` to `" → "` in the producer dropped storage and document overruns from every dry run while all eleven `platform-plans` e2e tests stayed green. | `apps/api-gateway/src/modules/platform/plan-usage.composer.ts` (`afterValue`), `apps/auth-service/src/modules/billing/plan-admin.service.ts` (`diff`) | A Super Admin lowering a plan limit is told fewer tenants are affected than actually are, and the under-report is silent. **Half-closed:** `plan-usage.composer.spec.ts` test 2 now asserts the producer still writes the separator the parser expects, so the reformat is red rather than invisible. What remains open is the shape — the projection should carry the target grants as NUMBERS rather than as a rendered string, which is what the plan-change block does and why it is not exposed to this. That is a proto change to `ApplyPlanResponse`, deliberately not folded into the plan-change work. |
| 22 | **`creditIssued`'s premise is unverified against a live Stripe downgrade, and it is a claim about money.** `prorationCredited` reads `latest_invoice.total < 0` off the subscription `subscriptions.update` returns with `expand: ['latest_invoice']`. `proration_behavior: 'always_invoice'` does not guarantee the proration lands there: Stripe may issue a downgrade credit as a customer credit balance transaction, leaving `latest_invoice` pointing at the PREVIOUS invoice — so `false` can be returned for a change that credited, and `true` can be read off an unrelated negative invoice on a straight upgrade. The e2e test asserts the mapping against a MOCKED `update`, so it pins the shape and not the premise. | `apps/auth-service/src/modules/billing/plan-change.service.ts` (`prorationCredited`), `libs/grpc-proto/src/proto/synapsedesk/auth/billing.proto` (`credit_issued`) | A tenant is told they were refunded when they were credited, or the reverse, on the page about their bill. **Bounded rather than open**: the field is three-valued — absent means *not determined* — so a client omits the sentence instead of printing a confident wrong one, and only an expanded invoice produces an answer at all. *Closing it:* one sandbox downgrade with a real card, inspect what `latest_invoice` actually holds, then narrow the field to a plain `bool` as a deliberate edit. Nothing in this repository can settle it. |
| 23 | **The FCM error-code mapping is verified against a fixture, not against Firebase.** `isDeadTokenError` deletes a stored device token for exactly two codes — `messaging/registration-token-not-registered` and `messaging/invalid-registration-token` — and keeps it for everything else. There is no client in this repository (seven services, no mobile app) and no live FCM in any test environment, so the mapping is asserted at a mocked `sendEachForMulticast` boundary: the tests prove the code BRANCHES correctly on those strings, not that those strings are what Firebase actually sends. | `apps/notification-service/src/modules/in-app/in-app-notification.service.ts` (`DEAD_TOKEN_ERRORS`), `apps/notification-service/test/modules/push.e2e-spec.ts` | This is the one path where the system destroys a stored credential because a third party said so. **The two directions are not symmetric**: a code wrongly absent from the set means a dead token lingers and every send to it fails harmlessly, while a code wrongly present means a live device is silently unsubscribed with no error and no way back except reinstalling the app — which is why the set is a literal rather than a prefix match, and why an unknown code KEEPS. Same shape as #22's `creditIssued`: the premise cannot be settled from this repository. *Closing it:* one send against a real FCM project with a deliberately uninstalled app, and compare the code returned. |
| 24 | **`estimatedMrr` is our arithmetic over Stripe's data, and Stripe computes the same figure differently and authoritatively.** The hourly snapshot sums `unit_amount x quantity` over `status: 'active'` subscriptions, dividing annual prices by twelve. Three known divergences travel on the wire in `excludes` — discounts (Stripe applies coupons and this does not, so ours reads HIGH), trials (`trialing` is excluded here while the tenant status mapping folds it into `ACTIVE`), and past-due. The arithmetic is tested against a mocked `subscriptions.list`, which pins the shape and not the premise: **nothing in this repository can compare the output to Stripe's own MRR**, which is the only comparison that would say whether those three are the only three. | `apps/auth-service/src/modules/finance/billing-snapshot.job.ts` (`compute`, `monthlyAmount`), `libs/common/src/configs/billing.config.ts` (`REVENUE_EXCLUSIONS`) | **Same shape as #22 and #23**: a premise that cannot be settled from inside the system. Bounded rather than open — the field is named `estimatedMrr` rather than `mrr`, a mixed-currency or unpriceable set refuses instead of producing a total, and `excludes` is on every response including the degraded ones, so a client cannot render the number without the caveat. *Closing it:* open Stripe's Billing dashboard for the same account on the same day and compare. If the two agree, narrow the caveats; if they do not, the difference names the fourth exclusion. **The honest answer may be a link rather than a better formula** — Stripe already computes this properly, and a figure that must be authoritative should come from where it is authoritative. |
| 25 | **`billing_events` is only as complete as an `enabled_events` list nothing in this repository sets.** Every figure in `GET /platform/finance/events` — new subscriptions, cancellations, failed payments — counts rows written when a webhook arrives. Which event types arrive at all is configured on the Stripe **endpoint**, and `scripts/provision-stripe.mjs` creates Products, Prices and the portal configuration and **never touches `webhookEndpoints`**. The endpoint is created by hand in the Dashboard, or by `stripe listen` in development, or not at all. | `scripts/provision-stripe.mjs`, `apps/auth-service/src/modules/billing/entitlement-writer.service.ts` (`claim`, which records EVERY verified event), `apps/auth-service/src/modules/finance/finance.service.ts` (`listEvents`) | **"No cancellations this month" and "cancellations were never enabled" render identically** — a zero on a chart, with nothing to distinguish them. That is the silent-zero failure `evaluated_dimensions` was added to prevent for plan applies, in a surface that had no equivalent. **Half-closed:** `observedEventTypes` reports the types actually seen in the range, so a type that has never arrived is visibly absent rather than reported as zero — a reader who looks can tell. What remains open is that nobody has to look. *Closing it:* extend `provision-stripe.mjs` to create the webhook endpoint with an explicit `enabled_events` list, putting the configuration in version control. Same class as #20, one endpoint further along: an input that decides what the system knows, living outside the repository. |
| 26 | **The SSRF deny-list is IP-range arithmetic in one file, not a vetted library, and it is the whole security boundary of outbound webhooks.** `addressIsDenied` refuses RFC1918, loopback, link-local, CGNAT, the IPv6 equivalents and IPv4-mapped forms — checked at connection time inside the socket's `lookup`, on every resolved address, with the connection pinned to a checked one so DNS rebinding has no second resolution to exploit. It is unit-tested address by address and closes the holes a string check leaves. **What it cannot promise is completeness against a determined attacker**: a novel encoding, an address class not enumerated, or a platform whose resolver returns a form the parser does not expect would pass. | `apps/notification-service/src/modules/webhooks/webhook-target.guard.ts` (`addressIsDenied`, `buildGuardedLookup`), `webhook-sender.service.ts` | This is the first outbound HTTP to a caller-supplied host in the repository, so there is no prior art and no shared library to lean on — every other `fetch`/`request` trusts its destination by construction. **The durable answer is egress control the process cannot bypass** — a NetworkPolicy or egress proxy that refuses the metadata endpoint and the mesh regardless of what the application resolves — which is a deployment artifact this repo does not contain (the same shape as gaps #15–#17). Until then the guard is the boundary, and the `test` route sends through the same guard precisely so it cannot become a softer path around it. *Closing it:* a NetworkPolicy denying RFC1918/link-local egress from the notification pod, plus keeping the deny-list as defence in depth. |
