# GENERATED by scripts/generate-job-alerts.mjs — do not edit.
#
# Staleness alerting for every job in SCHEDULED_JOBS via
# Regenerate with:
#
#     npm run build -w @synapsedesk/common && node scripts/generate-job-alerts.mjs
groups:
  - name: synapsedesk-scheduled-jobs
    rules:
      # cron: 0 2 * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="analytics-daily"} > 172800
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 172800s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="analytics-daily"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'analytics-daily has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.

      # cron: 0 3 * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="auth-daily"} > 172800
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 172800s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="auth-daily"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'auth-daily has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.

      # cron: 0 * * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="auth-hourly"} > 7200
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 7200s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="auth-hourly"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'auth-hourly has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.

      # cron: */10 * * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="ingestion-reconcile"} > 1200
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 1200s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="ingestion-reconcile"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'ingestion-reconcile has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.

      # cron: 0 2 * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="ledger-daily"} > 172800
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 172800s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="ledger-daily"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'ledger-daily has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.

      # cron: 0 * * * *
      - alert: ScheduledJobStale
        expr: time() - job_last_success_timestamp_seconds{job="ledger-hourly"} > 7200
        for: 10m
        labels: { severity: warning }
        annotations:
          summary: '{{ $labels.job }} has not succeeded in over 7200s'
          description: >-
            The job last succeeded at {{ $value | humanizeTimestamp }}. This
            fires for "it broke" and for "the schedule was never wired" alike —
            the two cases that are indistinguishable from outside and equally bad.

      # **A job with NO SERIES at all**, which the gauge exports as ABSENT rather
      # than as zero. Zero would be 1970 and would satisfy the rule above, so
      # without this the never-wired case would look like the broken case; with
      # it, the two alert separately and get different responses.
      - alert: ScheduledJobMissing
        expr: absent(job_last_success_timestamp_seconds{job="ledger-hourly"})
        for: 30m
        labels: { severity: critical }
        annotations:
          summary: 'ledger-hourly has never reported a successful run'
          description: >-
            No heartbeat exists for this job. Either the scheduler was never
            registered, or the owning service has never been scraped.
