-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0

# Lints and import-sorts the Python half. Configured in pyproject under
# [tool.ruff]; the rule set is deliberately narrow — see the comment there.
ruff==0.16.1

# Generates `*_pb2_grpc.pyi`. Without it the servicer base class is unannotated
# — its methods `raise NotImplementedError`, so a checker infers `-> Never` and
# reports every real implementation as an inconsistent override.
mypy-protobuf==5.1.0

# Type checker. `[tool.pyright]` in pyproject is what points it (and Pylance)
# at this service's venv rather than the workspace interpreter.
pyrefly==1.2.0

# grpc ships no inline type information, so a checker reads every `grpc.*` as
# untyped and silently stops verifying that half of the servicer. Stubs only —
# nothing imports this at runtime.
types-grpcio==1.83.0.20260730

# The health-checking half, for the ops surface. Covers
# `health_pb2` and `health_pb2_grpc`; it does NOT stub `grpc_health.v1._async`,
# which is why `server.py` still carries a note there rather than an import fix.
types-grpcio-health-checking==1.0.0.20260518

# The eval harness (`npm run eval:rag`) reads its golden set from YAML
# A dev dependency rather than a runtime one: the harness is run
# deliberately, by a person, and never by the service.
PyYAML==6.0.2

# Language identification for the eval's fourth metric. OPTIONAL at runtime —
# `eval/run.py` falls back to marker words if the import fails, because a
# harness that cannot start without a dependency is a harness nobody runs.
langdetect==1.0.9
