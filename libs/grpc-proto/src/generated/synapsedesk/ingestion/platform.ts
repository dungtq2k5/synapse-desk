// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/ingestion/platform.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

/**
 * Usage for MANY tenants at once.
 *
 * Batched on purpose: the plan-apply projection asks about every subscriber of
 * a plan, and N sequential calls from a caller with no tenant is precisely the
 * shape this surface exists to avoid.
 */
export interface PlatformUsageRequest {
  organizationIds: string[];
}

export interface TenantUsage {
  organizationId: string;
  usedBytes: number;
  documentCount: number;
}

/**
 * *One row per REQUESTED id, zero-filled** — never one row per id that happens
 * to own a document.
 *
 * A tenant with no documents produces no `groupBy` row, and a sparse response
 * would make the caller decide what absence means — and the two readings
 * available, "this tenant is under the limit" and "we did not check this
 * tenant", must not arrive as the same message. The request
 * carries the id list, so the surface can make the sparse case unrepresentable
 * and does.
 */
export interface PlatformUsageResponse {
  usage: TenantUsage[];
}

export interface IngestionPlatformServiceClient {
  getPlatformUsage(request: PlatformUsageRequest, metadata?: Metadata): Observable<PlatformUsageResponse>;
}

export interface IngestionPlatformServiceController {
  getPlatformUsage(
    request: PlatformUsageRequest,
    metadata?: Metadata,
  ): Promise<PlatformUsageResponse> | Observable<PlatformUsageResponse> | PlatformUsageResponse;
}

export function IngestionPlatformServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["getPlatformUsage"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("IngestionPlatformService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("IngestionPlatformService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const INGESTION_PLATFORM_SERVICE_NAME = "IngestionPlatformService";
