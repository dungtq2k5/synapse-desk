// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/auth/department.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PageMeta, PageRequest, UserResponse } from "./common";

export interface DepartmentResponse {
  id: string;
  name: string;
  description?:
    | string
    | undefined;
  /**
   * Computed via Prisma's `_count`, so listing N departments stays one query
   * rather than N+1.
   */
  memberCount: number;
  /**
   * Populated only for soft-deleted rows, so a list including them can be
   * rendered differently rather than silently showing them as live.
   */
  deletedAt?: Timestamp | undefined;
  deletedByName?: string | undefined;
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface ListDepartmentsRequest {
  page:
    | PageRequest
    | undefined;
  /**
   * Honoured only when the caller holds department.delete — the gateway checks
   * that; this service trusts the flag it is handed.
   */
  includeDeleted: boolean;
}

export interface ListDepartmentsResponse {
  items: DepartmentResponse[];
  meta: PageMeta | undefined;
}

export interface GetDepartmentRequest {
  id: string;
}

export interface CreateDepartmentRequest {
  name: string;
  description?: string | undefined;
}

export interface UpdateDepartmentRequest {
  id: string;
  /**
   * Absent means "leave unchanged" -- distinct from an empty description, which
   * means "clear it". A non-optional string could not express the difference.
   */
  name?: string | undefined;
  description?: string | undefined;
}

export interface DepartmentIdRequest {
  id: string;
}

export interface DeleteDepartmentResponse {
}

export interface DepartmentMemberResponse {
  user: UserResponse | undefined;
  isPrimary: boolean;
  assignedByName?: string | undefined;
  assignedAt: Timestamp | undefined;
}

export interface ListDepartmentMembersRequest {
  departmentId: string;
  page: PageRequest | undefined;
}

export interface ListDepartmentMembersResponse {
  items: DepartmentMemberResponse[];
  meta: PageMeta | undefined;
}

export interface AddDepartmentMembersRequest {
  departmentId: string;
  userIds: string[];
  /**
   * Applies to every id in the batch. Setting it for several users is legal --
   * the partial unique index is per USER, not per department.
   */
  isPrimary: boolean;
}

export interface AddDepartmentMembersResponse {
  addedCount: number;
  updatedCount: number;
}

export interface RemoveDepartmentMemberRequest {
  departmentId: string;
  userId: string;
}

export interface RemoveDepartmentMemberResponse {
}

/**
 * ---------------------------------------------------------------------------
 * `ListXByIds` — the contract behind every DataLoader (27-doc §1).
 *
 * Six properties, and five of them are things DataLoader depends on:
 *
 *   1. TENANT-SCOPED from the caller context, never from a request field. The
 *      id is a loader's cache key and carries no tenant, so this RPC is the
 *      only thing standing between a uuid and another tenant's row.
 *   2. MISSING IDS ARE OMITTED, never an error. A batch of 50 where one row was
 *      deleted returns 49; erroring fails 50 fields for one absent row.
 *   3. DUPLICATE IDS COLLAPSE. `IN (…)` handles it; the response must not
 *      double-count.
 *   4. EMPTY REQUEST -> EMPTY RESPONSE, not an error. A page where nothing has
 *      an assignee is a valid page.
 *   5. BATCH SIZE CAPPED, and the cap is an ERROR rather than a truncation —
 *      truncation is indistinguishable from missing data.
 *   6. ORDER IS NOT GUARANTEED. The response is a SET; the caller maps it back
 *      onto its keys (27-doc §2), because a database returns `WHERE id IN
 *      ('c','a','b')` as a, b, c and handing that straight to DataLoader
 *      renders the wrong entity against every key.
 *
 * SOFT DELETES ARE VISIBLE BY ID. A ticket citing a deleted user still needs to
 * render someone; omitting it makes property 2 fire for a row that exists.
 * ---------------------------------------------------------------------------
 */
export interface ListDepartmentsByIdsRequest {
  departmentIds: string[];
}

export interface ListDepartmentsByIdsResponse {
  items: DepartmentResponse[];
}

export interface DepartmentServiceClient {
  listDepartmentsByIds(
    request: ListDepartmentsByIdsRequest,
    metadata?: Metadata,
  ): Observable<ListDepartmentsByIdsResponse>;

  listDepartments(request: ListDepartmentsRequest, metadata?: Metadata): Observable<ListDepartmentsResponse>;

  getDepartment(request: GetDepartmentRequest, metadata?: Metadata): Observable<DepartmentResponse>;

  createDepartment(request: CreateDepartmentRequest, metadata?: Metadata): Observable<DepartmentResponse>;

  updateDepartment(request: UpdateDepartmentRequest, metadata?: Metadata): Observable<DepartmentResponse>;

  deleteDepartment(request: DepartmentIdRequest, metadata?: Metadata): Observable<DeleteDepartmentResponse>;

  restoreDepartment(request: DepartmentIdRequest, metadata?: Metadata): Observable<DepartmentResponse>;

  listDepartmentMembers(
    request: ListDepartmentMembersRequest,
    metadata?: Metadata,
  ): Observable<ListDepartmentMembersResponse>;

  addDepartmentMembers(
    request: AddDepartmentMembersRequest,
    metadata?: Metadata,
  ): Observable<AddDepartmentMembersResponse>;

  removeDepartmentMember(
    request: RemoveDepartmentMemberRequest,
    metadata?: Metadata,
  ): Observable<RemoveDepartmentMemberResponse>;
}

export interface DepartmentServiceController {
  listDepartmentsByIds(
    request: ListDepartmentsByIdsRequest,
    metadata?: Metadata,
  ): Promise<ListDepartmentsByIdsResponse> | Observable<ListDepartmentsByIdsResponse> | ListDepartmentsByIdsResponse;

  listDepartments(
    request: ListDepartmentsRequest,
    metadata?: Metadata,
  ): Promise<ListDepartmentsResponse> | Observable<ListDepartmentsResponse> | ListDepartmentsResponse;

  getDepartment(
    request: GetDepartmentRequest,
    metadata?: Metadata,
  ): Promise<DepartmentResponse> | Observable<DepartmentResponse> | DepartmentResponse;

  createDepartment(
    request: CreateDepartmentRequest,
    metadata?: Metadata,
  ): Promise<DepartmentResponse> | Observable<DepartmentResponse> | DepartmentResponse;

  updateDepartment(
    request: UpdateDepartmentRequest,
    metadata?: Metadata,
  ): Promise<DepartmentResponse> | Observable<DepartmentResponse> | DepartmentResponse;

  deleteDepartment(
    request: DepartmentIdRequest,
    metadata?: Metadata,
  ): Promise<DeleteDepartmentResponse> | Observable<DeleteDepartmentResponse> | DeleteDepartmentResponse;

  restoreDepartment(
    request: DepartmentIdRequest,
    metadata?: Metadata,
  ): Promise<DepartmentResponse> | Observable<DepartmentResponse> | DepartmentResponse;

  listDepartmentMembers(
    request: ListDepartmentMembersRequest,
    metadata?: Metadata,
  ): Promise<ListDepartmentMembersResponse> | Observable<ListDepartmentMembersResponse> | ListDepartmentMembersResponse;

  addDepartmentMembers(
    request: AddDepartmentMembersRequest,
    metadata?: Metadata,
  ): Promise<AddDepartmentMembersResponse> | Observable<AddDepartmentMembersResponse> | AddDepartmentMembersResponse;

  removeDepartmentMember(
    request: RemoveDepartmentMemberRequest,
    metadata?: Metadata,
  ):
    | Promise<RemoveDepartmentMemberResponse>
    | Observable<RemoveDepartmentMemberResponse>
    | RemoveDepartmentMemberResponse;
}

export function DepartmentServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "listDepartmentsByIds",
      "listDepartments",
      "getDepartment",
      "createDepartment",
      "updateDepartment",
      "deleteDepartment",
      "restoreDepartment",
      "listDepartmentMembers",
      "addDepartmentMembers",
      "removeDepartmentMember",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("DepartmentService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("DepartmentService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const DEPARTMENT_SERVICE_NAME = "DepartmentService";
