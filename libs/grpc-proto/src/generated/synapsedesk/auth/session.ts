// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/auth/session.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";

export interface SessionResponse {
  id: string;
  deviceName?: string | undefined;
  ipAddress: string;
  userAgent: string;
  /**
   * True for the session the request itself arrived on, matched by FAMILY id
   * against the presented refresh token -- not by IP or user agent, which two
   * browsers on one machine share.
   */
  current: boolean;
  isTrusted: boolean;
  trustedUntil?: Timestamp | undefined;
  expiresAt: Timestamp | undefined;
  createdAt: Timestamp | undefined;
}

export interface ListSessionsRequest {
  /**
   * The caller's presented refresh token, used ONLY to mark `current`. Optional
   * because a caller may list sessions without holding one (a fresh access
   * token from a 2FA flow); the list is simply returned with nothing marked.
   */
  refreshToken?: string | undefined;
}

export interface ListSessionsResponse {
  items: SessionResponse[];
}

export interface SessionIdRequest {
  sessionId: string;
  /**
   * The caller's own refresh token, so the service can report whether the
   * revoked family was the one this request arrived on. The gateway cannot work
   * that out itself -- it never sees a family id -- and it needs the answer to
   * decide whether to clear the caller's cookies.
   */
  refreshToken?: string | undefined;
}

export interface RevokeSessionResponse {
  /**
   * True when the revoked family is the caller's own, so the gateway knows to
   * clear cookies. The gateway cannot work this out for itself: it never sees
   * the family id.
   */
  wasCurrent: boolean;
  revokedCount: number;
}

export interface RevokeTrustResponse {
  untrustedCount: number;
}

/**
 * Distinct from ListSessionsRequest despite being empty today: reusing that
 * message would tie "revoke all trust" to a field that exists only to mark the
 * current session, and adding one there later would silently change this RPC's
 * contract.
 */
export interface RevokeAllTrustRequest {
}

export interface ListUserSessionsRequest {
  userId: string;
}

export interface RevokeUserSessionsRequest {
  userId: string;
}

export interface RevokeUserSessionsResponse {
  revokedCount: number;
}

export interface SessionServiceClient {
  listSessions(request: ListSessionsRequest, metadata?: Metadata): Observable<ListSessionsResponse>;

  revokeSession(request: SessionIdRequest, metadata?: Metadata): Observable<RevokeSessionResponse>;

  revokeSessionTrust(request: SessionIdRequest, metadata?: Metadata): Observable<RevokeTrustResponse>;

  revokeAllTrust(request: RevokeAllTrustRequest, metadata?: Metadata): Observable<RevokeTrustResponse>;

  listUserSessions(request: ListUserSessionsRequest, metadata?: Metadata): Observable<ListSessionsResponse>;

  revokeUserSessions(request: RevokeUserSessionsRequest, metadata?: Metadata): Observable<RevokeUserSessionsResponse>;
}

export interface SessionServiceController {
  listSessions(
    request: ListSessionsRequest,
    metadata?: Metadata,
  ): Promise<ListSessionsResponse> | Observable<ListSessionsResponse> | ListSessionsResponse;

  revokeSession(
    request: SessionIdRequest,
    metadata?: Metadata,
  ): Promise<RevokeSessionResponse> | Observable<RevokeSessionResponse> | RevokeSessionResponse;

  revokeSessionTrust(
    request: SessionIdRequest,
    metadata?: Metadata,
  ): Promise<RevokeTrustResponse> | Observable<RevokeTrustResponse> | RevokeTrustResponse;

  revokeAllTrust(
    request: RevokeAllTrustRequest,
    metadata?: Metadata,
  ): Promise<RevokeTrustResponse> | Observable<RevokeTrustResponse> | RevokeTrustResponse;

  listUserSessions(
    request: ListUserSessionsRequest,
    metadata?: Metadata,
  ): Promise<ListSessionsResponse> | Observable<ListSessionsResponse> | ListSessionsResponse;

  revokeUserSessions(
    request: RevokeUserSessionsRequest,
    metadata?: Metadata,
  ): Promise<RevokeUserSessionsResponse> | Observable<RevokeUserSessionsResponse> | RevokeUserSessionsResponse;
}

export function SessionServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "listSessions",
      "revokeSession",
      "revokeSessionTrust",
      "revokeAllTrust",
      "listUserSessions",
      "revokeUserSessions",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("SessionService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("SessionService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const SESSION_SERVICE_NAME = "SessionService";
