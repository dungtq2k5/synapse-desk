// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/auth/billing.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";
import { AiModelTier, OrgStatus } from "./common";

/**
 * The webhook, forwarded from the gateway.
 *
 * **`payload` is the RAW BYTES, not a parsed object**, and that is the whole
 * point of this message shape. Stripe's signature is computed over the exact
 * bytes of the request; a JSON round trip through the gateway would re-serialize
 * them — different key order, different whitespace — and verification would
 * fail for every event forever (14-doc §3.2).
 *
 * Verification happens HERE rather than at the gateway, so the webhook secret
 * lives with the service that owns the rows it authorizes writes to. The
 * gateway stays a pipe that cannot forge an entitlement change.
 */
export interface StripeWebhookRequest {
  payload: Uint8Array;
  /** Stripe's `Stripe-Signature` header, verbatim. */
  signature: string;
}

export interface StripeWebhookResponse {
  /**
   * BillingEventStatus, or empty when the event type is one we do not act on.
   * Stripe only needs a 2xx; this is for the gateway's log and for tests.
   */
  status: string;
  /** Present when a `billing_events` row was written. */
  billingEventId?: string | undefined;
}

export interface SubscriptionResponse {
  /**
   * Absent for a grandfathered tenant — free, internal, or onboarded before
   * billing existed. NOT an error: it will be every tenant on the day this
   * ships, and a 500 there would take the billing page down for all of them.
   */
  stripeCustomerId?: string | undefined;
  stripeSubscriptionId?:
    | string
    | undefined;
  /** The plan's label, for display only. Never an authorization input. */
  planName: string;
  /**
   * The five ENTITLEMENTS, read from Postgres. Never from Stripe: a dashboard
   * that fans out to a third party on every load fails when they do.
   */
  maxAgentSeats: number;
  maxStorageBytes: number;
  monthlyAiTokenBudget: number;
  aiModelTier: AiModelTier;
  billingCycleStart:
    | Timestamp
    | undefined;
  /** The tenant lifecycle status the subscription drove. */
  status: OrgStatus;
}

export interface CreateCheckoutSessionRequest {
  priceId: string;
  successUrl: string;
  cancelUrl: string;
}

/**
 * A URL and nothing else. **Checkout grants nothing** (14-doc §4): entitlements
 * are written when the webhook confirms, so a user who closes the tab mid-flow
 * is not upgraded and a user who pays does not depend on their browser making
 * it back to a redirect.
 */
export interface CheckoutSessionResponse {
  url: string;
}

export interface CreatePortalSessionRequest {
  returnUrl: string;
}

export interface PortalSessionResponse {
  url: string;
}

export interface InvoiceResponse {
  id: string;
  number: string;
  amountDue: number;
  currency: string;
  status: string;
  created: Timestamp | undefined;
  hostedInvoiceUrl: string;
}

/**
 * The ONE place a live Stripe read is correct, because invoices are
 * deliberately not mirrored.
 */
export interface ListInvoicesResponse {
  items: InvoiceResponse[];
}

export interface ListInvoicesRequest {
  limit: number;
}

export interface BillingEmptyRequest {
}

export interface BillingServiceClient {
  /**
   * *Bypasses the auth guard, the lifecycle gate, tenant scoping and
   * per-tenant rate limiting** — 14-doc §3.3, and each for a different reason.
   * The lifecycle bypass is the one that matters most: this endpoint WRITES
   * `organizations.status`, so gating it on that status would make suspension
   * a one-way door.
   */

  handleStripeWebhook(request: StripeWebhookRequest, metadata?: Metadata): Observable<StripeWebhookResponse>;

  getSubscription(request: BillingEmptyRequest, metadata?: Metadata): Observable<SubscriptionResponse>;

  createCheckoutSession(
    request: CreateCheckoutSessionRequest,
    metadata?: Metadata,
  ): Observable<CheckoutSessionResponse>;

  createPortalSession(request: CreatePortalSessionRequest, metadata?: Metadata): Observable<PortalSessionResponse>;

  listInvoices(request: ListInvoicesRequest, metadata?: Metadata): Observable<ListInvoicesResponse>;
}

export interface BillingServiceController {
  /**
   * *Bypasses the auth guard, the lifecycle gate, tenant scoping and
   * per-tenant rate limiting** — 14-doc §3.3, and each for a different reason.
   * The lifecycle bypass is the one that matters most: this endpoint WRITES
   * `organizations.status`, so gating it on that status would make suspension
   * a one-way door.
   */

  handleStripeWebhook(
    request: StripeWebhookRequest,
    metadata?: Metadata,
  ): Promise<StripeWebhookResponse> | Observable<StripeWebhookResponse> | StripeWebhookResponse;

  getSubscription(
    request: BillingEmptyRequest,
    metadata?: Metadata,
  ): Promise<SubscriptionResponse> | Observable<SubscriptionResponse> | SubscriptionResponse;

  createCheckoutSession(
    request: CreateCheckoutSessionRequest,
    metadata?: Metadata,
  ): Promise<CheckoutSessionResponse> | Observable<CheckoutSessionResponse> | CheckoutSessionResponse;

  createPortalSession(
    request: CreatePortalSessionRequest,
    metadata?: Metadata,
  ): Promise<PortalSessionResponse> | Observable<PortalSessionResponse> | PortalSessionResponse;

  listInvoices(
    request: ListInvoicesRequest,
    metadata?: Metadata,
  ): Promise<ListInvoicesResponse> | Observable<ListInvoicesResponse> | ListInvoicesResponse;
}

export function BillingServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "handleStripeWebhook",
      "getSubscription",
      "createCheckoutSession",
      "createPortalSession",
      "listInvoices",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("BillingService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("BillingService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const BILLING_SERVICE_NAME = "BillingService";
