// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/auth/role.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PageMeta, PageRequest } from "./common";

export interface RoleResponse {
  id: string;
  name: string;
  description?:
    | string
    | undefined;
  /**
   * True for a GLOBAL role: readable by every tenant, mutable by none. The
   * gateway uses it to grey out edit controls; the service enforces it.
   */
  isSystemRole: boolean;
  /**
   * Denormalized counter on `roles.user_assigned`, maintained in the same
   * transaction as every junction write. It is a CACHE -- authoritative enough
   * to gate deletion, not authoritative enough to be the only copy.
   */
  userAssigned: number;
  permissionCodes: string[];
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface ListRolesRequest {
  page:
    | PageRequest
    | undefined;
  /**
   * When false the response carries tenant roles only. Default (proto3 zero) is
   * false, so a caller that wants the assignable set must ask for it -- which
   * is the safer direction than silently widening a list.
   */
  includeSystem: boolean;
}

export interface ListRolesResponse {
  items: RoleResponse[];
  meta: PageMeta | undefined;
}

export interface RoleIdRequest {
  id: string;
}

export interface CreateRoleRequest {
  name: string;
  description?:
    | string
    | undefined;
  /** Applied at creation so a role is never briefly live with no permissions. */
  permissionCodes: string[];
}

export interface UpdateRoleRequest {
  id: string;
  /** Absent means "leave unchanged"; empty description means "clear it". */
  name?: string | undefined;
  description?: string | undefined;
}

export interface DeleteRoleResponse {
}

/**
 * REPLACE semantics, which is what makes it safe to retry: sending the same set
 * twice is a no-op rather than a double grant.
 */
export interface SetRolePermissionsRequest {
  id: string;
  permissionCodes: string[];
}

export interface PermissionResponse {
  id: string;
  code: string;
  name: string;
  /**
   * The `target` prefix of the code ('user' from 'user.read'). Derived, not
   * stored -- it is what makes the role editor groupable without a new column.
   */
  group: string;
}

export interface ListPermissionsRequest {
}

export interface ListPermissionsResponse {
  items: PermissionResponse[];
}

/**
 * Bulk-assign one role, leaving each user's other roles alone. The inverse
 * direction of SetUserRoles, which replaces a whole set for one user.
 */
export interface AssignRoleUsersRequest {
  roleId: string;
  userIds: string[];
}

export interface RevokeRoleUserRequest {
  roleId: string;
  userId: string;
}

export interface RoleServiceClient {
  listRoles(request: ListRolesRequest, metadata?: Metadata): Observable<ListRolesResponse>;

  getRole(request: RoleIdRequest, metadata?: Metadata): Observable<RoleResponse>;

  createRole(request: CreateRoleRequest, metadata?: Metadata): Observable<RoleResponse>;

  updateRole(request: UpdateRoleRequest, metadata?: Metadata): Observable<RoleResponse>;

  deleteRole(request: RoleIdRequest, metadata?: Metadata): Observable<DeleteRoleResponse>;

  setRolePermissions(request: SetRolePermissionsRequest, metadata?: Metadata): Observable<RoleResponse>;

  listPermissions(request: ListPermissionsRequest, metadata?: Metadata): Observable<ListPermissionsResponse>;

  /**
   * Both return the ROLE rather than a count: `user_assigned` is the number the
   * caller's screen is showing and the one this just changed. A bare
   * `{ assigned: n }` would leave the client guessing whether its total is
   * still right — and for a bulk that partially no-ops, comparing the returned
   * counter is the only way to learn how many were actually new.
   */

  assignRoleUsers(request: AssignRoleUsersRequest, metadata?: Metadata): Observable<RoleResponse>;

  revokeRoleUser(request: RevokeRoleUserRequest, metadata?: Metadata): Observable<RoleResponse>;
}

export interface RoleServiceController {
  listRoles(
    request: ListRolesRequest,
    metadata?: Metadata,
  ): Promise<ListRolesResponse> | Observable<ListRolesResponse> | ListRolesResponse;

  getRole(request: RoleIdRequest, metadata?: Metadata): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;

  createRole(
    request: CreateRoleRequest,
    metadata?: Metadata,
  ): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;

  updateRole(
    request: UpdateRoleRequest,
    metadata?: Metadata,
  ): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;

  deleteRole(
    request: RoleIdRequest,
    metadata?: Metadata,
  ): Promise<DeleteRoleResponse> | Observable<DeleteRoleResponse> | DeleteRoleResponse;

  setRolePermissions(
    request: SetRolePermissionsRequest,
    metadata?: Metadata,
  ): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;

  listPermissions(
    request: ListPermissionsRequest,
    metadata?: Metadata,
  ): Promise<ListPermissionsResponse> | Observable<ListPermissionsResponse> | ListPermissionsResponse;

  /**
   * Both return the ROLE rather than a count: `user_assigned` is the number the
   * caller's screen is showing and the one this just changed. A bare
   * `{ assigned: n }` would leave the client guessing whether its total is
   * still right — and for a bulk that partially no-ops, comparing the returned
   * counter is the only way to learn how many were actually new.
   */

  assignRoleUsers(
    request: AssignRoleUsersRequest,
    metadata?: Metadata,
  ): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;

  revokeRoleUser(
    request: RevokeRoleUserRequest,
    metadata?: Metadata,
  ): Promise<RoleResponse> | Observable<RoleResponse> | RoleResponse;
}

export function RoleServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "listRoles",
      "getRole",
      "createRole",
      "updateRole",
      "deleteRole",
      "setRolePermissions",
      "listPermissions",
      "assignRoleUsers",
      "revokeRoleUser",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("RoleService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("RoleService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const ROLE_SERVICE_NAME = "RoleService";
