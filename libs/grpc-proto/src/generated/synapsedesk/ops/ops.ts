// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/ops/ops.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

/**
 * `/version` for a service with no HTTP port.
 *
 * **Serve it from EVERY service, not just the gateway.** A rolling deploy where
 * one service lagged is precisely the state this diagnoses, and a gateway-only
 * version endpoint cannot see it — it would report the new SHA while the peer
 * still running the old code is the one causing the incident.
 *
 * A separate package from `grpc.health.v1` because that one is a copied
 * standard: adding a method to it would make the file no longer be the standard
 * while still claiming its package name, and the next person to re-copy the
 * upstream file would silently delete this.
 */
export interface VersionRequest {
}

export interface VersionResponse {
  version: string;
  sha: string;
  builtAt: string;
}

export interface OpsServiceClient {
  getVersion(request: VersionRequest, metadata?: Metadata): Observable<VersionResponse>;
}

export interface OpsServiceController {
  getVersion(
    request: VersionRequest,
    metadata?: Metadata,
  ): Promise<VersionResponse> | Observable<VersionResponse> | VersionResponse;
}

export function OpsServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["getVersion"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("OpsService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("OpsService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const OPS_SERVICE_NAME = "OpsService";
