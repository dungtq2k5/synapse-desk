// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/ticket/audit.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PageMeta, PageRequest } from "../auth/common";

export interface AuditLogResponse {
  id: string;
  /**
   * Absent for a platform-level act (RDM §1.7): the event belongs to the
   * platform, not to the customer it touched.
   */
  organizationId?: string | undefined;
  userId?: string | undefined;
  action: string;
  resourceType?: string | undefined;
  resourceId?: string | undefined;
  ipAddress?: string | undefined;
  userAgent?:
    | string
    | undefined;
  /**
   * JSON, carried as a string: the shape differs per action, so a typed message
   * could only be a `map<string, string>` that lost every nested value.
   */
  metadata: string;
  createdAt: Timestamp | undefined;
}

export interface ListAuditLogsRequest {
  page: PageRequest | undefined;
  action: string;
  userId: string;
  resourceType: string;
  resourceId: string;
  from?: Timestamp | undefined;
  to?:
    | Timestamp
    | undefined;
  /**
   * Platform scope: reads the `organization_id IS NULL` rows instead of a
   * tenant's. Refused for anyone but a Super Admin, in the service — never
   * mixed into a tenant's own view.
   */
  platformScope: boolean;
}

export interface ListAuditLogsResponse {
  items: AuditLogResponse[];
  meta: PageMeta | undefined;
}

export interface ListAuditActionsRequest {
  platformScope: boolean;
}

/**
 * Only the actions that ACTUALLY occurred for this tenant, not the full
 * AuditAction enum — a filter dropdown offering PLATFORM_* to a customer who
 * can never trigger it is a worse experience than a shorter list.
 */
export interface ListAuditActionsResponse {
  actions: string[];
}

export interface AuditServiceClient {
  listAuditLogs(request: ListAuditLogsRequest, metadata?: Metadata): Observable<ListAuditLogsResponse>;

  listAuditActions(request: ListAuditActionsRequest, metadata?: Metadata): Observable<ListAuditActionsResponse>;
}

export interface AuditServiceController {
  listAuditLogs(
    request: ListAuditLogsRequest,
    metadata?: Metadata,
  ): Promise<ListAuditLogsResponse> | Observable<ListAuditLogsResponse> | ListAuditLogsResponse;

  listAuditActions(
    request: ListAuditActionsRequest,
    metadata?: Metadata,
  ): Promise<ListAuditActionsResponse> | Observable<ListAuditActionsResponse> | ListAuditActionsResponse;
}

export function AuditServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["listAuditLogs", "listAuditActions"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("AuditService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("AuditService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const AUDIT_SERVICE_NAME = "AuditService";
