// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: synapsedesk/ticket/ai.proto

/* eslint-disable */
import type { Metadata } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Timestamp } from "../../google/protobuf/timestamp";
import { TicketPriority } from "./common";

export interface AiSummaryResponse {
  id: string;
  ticketId: string;
  summaryText: string;
  suggestedAction: string;
  confidenceScore: number;
  modelName: string;
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface TicketAiRequest {
  ticketId: string;
}

/**
 * Drafting takes the thread plus an optional steer from the agent ("shorter",
 * "offer the refund").
 */
export interface GenerateDraftRequest {
  ticketId: string;
  instruction?: string | undefined;
}

export interface GenerateDraftResponse {
  content: string;
  modelName: string;
  promptTokens: number;
  completionTokens: number;
  /**
   * The `ai_generations` row this draft was recorded as — doc 15 §3.2.
   *
   * The client hands it back as `generatedFromId` when the agent posts, which
   * is what closes the ACCEPTANCE LOOP. Without it the outcome is never
   * written, the hourly sweep marks the draft DISCARDED, and acceptance rate —
   * the one number justifying the co-pilot — counts a sent draft as ignored.
   */
  generationId: string;
  /** What the draft cited, so the agent can check it before pressing send. */
  citations: DraftCitation[];
}

export interface DraftCitation {
  chunkId: string;
  documentId: string;
  documentTitle: string;
  pageNumber?: number | undefined;
}

export interface SuggestionResponse {
  title: string;
  body: string;
  confidenceScore: number;
}

export interface GetSuggestionsResponse {
  items: SuggestionResponse[];
  /**
   * Carried through from rag-service — 39-doc §1. `items` is the next-step
   * list this endpoint already produced; these are the articles beside it.
   */
  articles: SuggestedArticleResponse[];
}

export interface SuggestedArticleResponse {
  documentId: string;
  documentTitle: string;
  pageNumber?: number | undefined;
  score: number;
}

export interface ClassifyTicketResponse {
  suggestedDepartmentId: string;
  /**
   * The enum this package already declares, rather than its NAME as a string.
   * A suggestion the agent can apply with one click must name a priority the
   * ticket surface accepts, and this is what makes the two the same type.
   */
  suggestedPriority: TicketPriority;
  confidenceScore: number;
}

export interface SimilarTicketResponse {
  ticketId: string;
  ticketNumber: number;
  title: string;
  similarityScore: number;
}

export interface ListSimilarTicketsResponse {
  items: SimilarTicketResponse[];
}

export interface AiServiceClient {
  /**
   * An UPSERT on the 1:1 relation — re-generating replaces the row rather than
   * appending a second summary of a thread that has since moved on.
   */

  generateSummary(request: TicketAiRequest, metadata?: Metadata): Observable<AiSummaryResponse>;

  getSummary(request: TicketAiRequest, metadata?: Metadata): Observable<AiSummaryResponse>;

  generateDraft(request: GenerateDraftRequest, metadata?: Metadata): Observable<GenerateDraftResponse>;

  getSuggestions(request: TicketAiRequest, metadata?: Metadata): Observable<GetSuggestionsResponse>;

  classifyTicket(request: TicketAiRequest, metadata?: Metadata): Observable<ClassifyTicketResponse>;

  listSimilarTickets(request: TicketAiRequest, metadata?: Metadata): Observable<ListSimilarTicketsResponse>;
}

export interface AiServiceController {
  /**
   * An UPSERT on the 1:1 relation — re-generating replaces the row rather than
   * appending a second summary of a thread that has since moved on.
   */

  generateSummary(
    request: TicketAiRequest,
    metadata?: Metadata,
  ): Promise<AiSummaryResponse> | Observable<AiSummaryResponse> | AiSummaryResponse;

  getSummary(
    request: TicketAiRequest,
    metadata?: Metadata,
  ): Promise<AiSummaryResponse> | Observable<AiSummaryResponse> | AiSummaryResponse;

  generateDraft(
    request: GenerateDraftRequest,
    metadata?: Metadata,
  ): Promise<GenerateDraftResponse> | Observable<GenerateDraftResponse> | GenerateDraftResponse;

  getSuggestions(
    request: TicketAiRequest,
    metadata?: Metadata,
  ): Promise<GetSuggestionsResponse> | Observable<GetSuggestionsResponse> | GetSuggestionsResponse;

  classifyTicket(
    request: TicketAiRequest,
    metadata?: Metadata,
  ): Promise<ClassifyTicketResponse> | Observable<ClassifyTicketResponse> | ClassifyTicketResponse;

  listSimilarTickets(
    request: TicketAiRequest,
    metadata?: Metadata,
  ): Promise<ListSimilarTicketsResponse> | Observable<ListSimilarTicketsResponse> | ListSimilarTicketsResponse;
}

export function AiServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "generateSummary",
      "getSummary",
      "generateDraft",
      "getSuggestions",
      "classifyTicket",
      "listSimilarTickets",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("AiService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("AiService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const AI_SERVICE_NAME = "AiService";
