# GENERATED by scripts/generate-k8s-config.mjs — do not edit.
#
# Source: apps/storage-service/.env.example (active AND commented lines — see the
# script header for why the commented ones are the cluster-relevant half).
# Regenerate with:
#
#     node scripts/generate-k8s-config.mjs
#
# ConfigMap: the non-secret half of the env contract.
#
# Documented and deliberately absent:
#   FIREBASE_STORAGE_EMULATOR_HOST — production uses real Firebase Storage; setting this points the SDK at an emulator that is not there
apiVersion: v1
kind: ConfigMap
metadata:
  name: storage-service-config
  labels:
    app.kubernetes.io/name: storage-service
data:
  FIREBASE_STORAGE_BUCKET: 'REPLACE_ME'
  FIREBASE_STORAGE_SERVICE_ACCOUNT_PATH: './serviceAccountKey.json'
  GRPC_HOST: '0.0.0.0'
  GRPC_PORT: '50253'
  NATS_URL: 'nats://nats:4222'
  NODE_ENV: 'production'
  READ_URL_TTL_SECONDS: '900'
  REDIS_DB: '0'
  UPLOAD_URL_TTL_SECONDS: '600'
